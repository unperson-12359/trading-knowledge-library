interface:
  display_name: "AR model"
  short_description: "Apply AR model with evidence and caveats"
  default_prompt: "Use $tkl-ar-model to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
