interface:
  display_name: "Selection bias"
  short_description: "Apply Selection bias with evidence and caveats"
  default_prompt: "Use $tkl-selection-bias to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
