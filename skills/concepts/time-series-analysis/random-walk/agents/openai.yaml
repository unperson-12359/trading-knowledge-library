interface:
  display_name: "Random walk"
  short_description: "Apply Random walk with evidence and caveats"
  default_prompt: "Use $tkl-random-walk to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
