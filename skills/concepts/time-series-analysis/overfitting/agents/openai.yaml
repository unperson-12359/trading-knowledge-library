interface:
  display_name: "Overfitting"
  short_description: "Apply Overfitting with evidence and caveats"
  default_prompt: "Use $tkl-overfitting to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
