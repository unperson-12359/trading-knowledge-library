interface:
  display_name: "White noise"
  short_description: "Apply White noise with evidence and caveats"
  default_prompt: "Use $tkl-white-noise to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
