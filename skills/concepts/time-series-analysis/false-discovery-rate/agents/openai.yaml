interface:
  display_name: "False discovery rate"
  short_description: "Apply False discovery rate with evidence and caveats"
  default_prompt: "Use $tkl-false-discovery-rate to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
