interface:
  display_name: "ARMA model"
  short_description: "Apply ARMA model with evidence and caveats"
  default_prompt: "Use $tkl-arma-model to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
