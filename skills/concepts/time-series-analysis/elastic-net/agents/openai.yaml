interface:
  display_name: "Elastic net"
  short_description: "Apply Elastic net with evidence and caveats"
  default_prompt: "Use $tkl-elastic-net to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
