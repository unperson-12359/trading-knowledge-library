interface:
  display_name: "Posterior distribution"
  short_description: "Apply Posterior distribution with evidence and caveats"
  default_prompt: "Use $tkl-posterior-distribution to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
