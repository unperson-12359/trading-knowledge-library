interface:
  display_name: "Look-ahead bias"
  short_description: "Apply Look-ahead bias with evidence and caveats"
  default_prompt: "Use $tkl-look-ahead-bias to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
