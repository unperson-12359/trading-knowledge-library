interface:
  display_name: "ARCH model"
  short_description: "Apply ARCH model with evidence and caveats"
  default_prompt: "Use $tkl-arch-model to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
