interface:
  display_name: "Unit root"
  short_description: "Apply Unit root with evidence and caveats"
  default_prompt: "Use $tkl-unit-root to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
