interface:
  display_name: "EGARCH model"
  short_description: "Apply EGARCH model with evidence and caveats"
  default_prompt: "Use $tkl-egarch-model to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
