interface:
  display_name: "Monte Carlo simulation"
  short_description: "Apply Monte Carlo simulation with evidence and caveats"
  default_prompt: "Use $tkl-monte-carlo-simulation to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
