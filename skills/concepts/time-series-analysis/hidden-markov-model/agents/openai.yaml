interface:
  display_name: "Hidden Markov model"
  short_description: "Apply Hidden Markov model with evidence and caveats"
  default_prompt: "Use $tkl-hidden-markov-model to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
