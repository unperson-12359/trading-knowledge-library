interface:
  display_name: "GARCH model"
  short_description: "Apply GARCH model with evidence and caveats"
  default_prompt: "Use $tkl-garch-model to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
