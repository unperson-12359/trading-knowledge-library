interface:
  display_name: "State-space model"
  short_description: "Apply State-space model with evidence and caveats"
  default_prompt: "Use $tkl-state-space-model to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
