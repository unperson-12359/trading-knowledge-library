interface:
  display_name: "MA model"
  short_description: "Apply MA model with evidence and caveats"
  default_prompt: "Use $tkl-ma-model to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
