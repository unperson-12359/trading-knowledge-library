interface:
  display_name: "Error-correction model"
  short_description: "Apply Error-correction model with evidence and caveats"
  default_prompt: "Use $tkl-error-correction-model to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
