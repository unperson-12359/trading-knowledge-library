interface:
  display_name: "Time series"
  short_description: "Apply Time series with evidence and caveats"
  default_prompt: "Use $tkl-time-series to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
