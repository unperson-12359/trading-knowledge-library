interface:
  display_name: "Bayesian inference"
  short_description: "Apply Bayesian inference with evidence and caveats"
  default_prompt: "Use $tkl-bayesian-inference to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
