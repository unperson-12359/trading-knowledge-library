interface:
  display_name: "Seasonal ARIMA"
  short_description: "Apply Seasonal ARIMA with evidence and caveats"
  default_prompt: "Use $tkl-seasonal-arima to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
