interface:
  display_name: "R-squared"
  short_description: "Apply R-squared with evidence and caveats"
  default_prompt: "Use $tkl-r-squared to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
