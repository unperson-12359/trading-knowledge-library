interface:
  display_name: "Residual"
  short_description: "Apply Residual with evidence and caveats"
  default_prompt: "Use $tkl-residual to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
