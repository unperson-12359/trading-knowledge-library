interface:
  display_name: "Prior distribution"
  short_description: "Apply Prior distribution with evidence and caveats"
  default_prompt: "Use $tkl-prior-distribution to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
