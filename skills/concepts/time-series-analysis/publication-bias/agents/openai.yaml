interface:
  display_name: "Publication bias"
  short_description: "Apply Publication bias with evidence and caveats"
  default_prompt: "Use $tkl-publication-bias to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
