interface:
  display_name: "Ordinary least squares"
  short_description: "Apply Ordinary least squares with evidence and caveats"
  default_prompt: "Use $tkl-ordinary-least-squares to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
