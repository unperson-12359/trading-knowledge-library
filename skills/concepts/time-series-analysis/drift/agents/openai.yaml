interface:
  display_name: "Drift"
  short_description: "Apply Drift with evidence and caveats"
  default_prompt: "Use $tkl-drift to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
