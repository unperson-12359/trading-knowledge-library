interface:
  display_name: "Regime-switching model"
  short_description: "Apply Regime-switching model with evidence and caveats"
  default_prompt: "Use $tkl-regime-switching-model to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
