interface:
  display_name: "Likelihood function"
  short_description: "Apply Likelihood function with evidence and caveats"
  default_prompt: "Use $tkl-likelihood-function to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
