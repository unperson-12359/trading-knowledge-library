interface:
  display_name: "ARIMA model"
  short_description: "Apply ARIMA model with evidence and caveats"
  default_prompt: "Use $tkl-arima-model to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
