interface:
  display_name: "Kalman filter"
  short_description: "Apply Kalman filter with evidence and caveats"
  default_prompt: "Use $tkl-kalman-filter to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
