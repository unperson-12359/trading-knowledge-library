interface:
  display_name: "Maximum likelihood estimation"
  short_description: "Apply Maximum likelihood estimation with evidence and caveats"
  default_prompt: "Use $tkl-maximum-likelihood-estimation to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
