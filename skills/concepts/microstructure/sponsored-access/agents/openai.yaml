interface:
  display_name: "Sponsored access"
  short_description: "Apply Sponsored access with evidence and caveats"
  default_prompt: "Use $tkl-sponsored-access to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
