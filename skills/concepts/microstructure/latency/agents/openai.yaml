interface:
  display_name: "Latency"
  short_description: "Apply Latency with evidence and caveats"
  default_prompt: "Use $tkl-latency to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
