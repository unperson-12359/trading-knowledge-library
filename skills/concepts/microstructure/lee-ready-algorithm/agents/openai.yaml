interface:
  display_name: "Lee-Ready algorithm"
  short_description: "Apply Lee-Ready algorithm with evidence and caveats"
  default_prompt: "Use $tkl-lee-ready-algorithm to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
