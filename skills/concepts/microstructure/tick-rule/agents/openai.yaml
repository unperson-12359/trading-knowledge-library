interface:
  display_name: "Tick rule"
  short_description: "Apply Tick rule with evidence and caveats"
  default_prompt: "Use $tkl-tick-rule to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
