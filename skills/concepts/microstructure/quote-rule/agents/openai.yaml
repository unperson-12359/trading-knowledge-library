interface:
  display_name: "Quote rule"
  short_description: "Apply Quote rule with evidence and caveats"
  default_prompt: "Use $tkl-quote-rule to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
