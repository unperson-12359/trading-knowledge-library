interface:
  display_name: "Order book"
  short_description: "Apply Order book with evidence and caveats"
  default_prompt: "Use $tkl-order-book to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
