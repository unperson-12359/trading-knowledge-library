interface:
  display_name: "Adverse selection"
  short_description: "Apply Adverse selection with evidence and caveats"
  default_prompt: "Use $tkl-adverse-selection to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
