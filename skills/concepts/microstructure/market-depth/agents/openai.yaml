interface:
  display_name: "Market depth"
  short_description: "Apply Market depth with evidence and caveats"
  default_prompt: "Use $tkl-market-depth to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
