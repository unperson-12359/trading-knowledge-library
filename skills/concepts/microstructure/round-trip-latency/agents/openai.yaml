interface:
  display_name: "Round-trip latency"
  short_description: "Apply Round-trip latency with evidence and caveats"
  default_prompt: "Use $tkl-round-trip-latency to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
