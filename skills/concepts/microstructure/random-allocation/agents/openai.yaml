interface:
  display_name: "Random allocation"
  short_description: "Apply Random allocation with evidence and caveats"
  default_prompt: "Use $tkl-random-allocation to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
