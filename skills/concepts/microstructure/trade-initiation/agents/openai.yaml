interface:
  display_name: "Trade initiation"
  short_description: "Apply Trade initiation with evidence and caveats"
  default_prompt: "Use $tkl-trade-initiation to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
