interface:
  display_name: "Queue imbalance"
  short_description: "Apply Queue imbalance with evidence and caveats"
  default_prompt: "Use $tkl-queue-imbalance to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
