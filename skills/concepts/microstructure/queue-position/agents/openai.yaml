interface:
  display_name: "Queue position"
  short_description: "Apply Queue position with evidence and caveats"
  default_prompt: "Use $tkl-queue-position to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
