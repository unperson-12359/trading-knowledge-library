interface:
  display_name: "Stale quote"
  short_description: "Apply Stale quote with evidence and caveats"
  default_prompt: "Use $tkl-stale-quote to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
