interface:
  display_name: "Price-time priority"
  short_description: "Apply Price-time priority with evidence and caveats"
  default_prompt: "Use $tkl-price-time-priority to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
