interface:
  display_name: "Midpoint"
  short_description: "Apply Midpoint with evidence and caveats"
  default_prompt: "Use $tkl-midpoint to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
