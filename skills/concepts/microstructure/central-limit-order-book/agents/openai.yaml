interface:
  display_name: "Central limit order book"
  short_description: "Apply Central limit order book with evidence and caveats"
  default_prompt: "Use $tkl-central-limit-order-book to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
