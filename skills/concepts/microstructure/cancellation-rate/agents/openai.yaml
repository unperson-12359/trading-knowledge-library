interface:
  display_name: "Cancellation rate"
  short_description: "Apply Cancellation rate with evidence and caveats"
  default_prompt: "Use $tkl-cancellation-rate to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
