interface:
  display_name: "Size-time priority"
  short_description: "Apply Size-time priority with evidence and caveats"
  default_prompt: "Use $tkl-size-time-priority to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
