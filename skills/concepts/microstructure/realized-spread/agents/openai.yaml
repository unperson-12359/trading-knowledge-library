interface:
  display_name: "Realized spread"
  short_description: "Apply Realized spread with evidence and caveats"
  default_prompt: "Use $tkl-realized-spread to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
