interface:
  display_name: "Signed volume"
  short_description: "Apply Signed volume with evidence and caveats"
  default_prompt: "Use $tkl-signed-volume to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
