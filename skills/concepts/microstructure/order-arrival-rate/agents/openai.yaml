interface:
  display_name: "Order arrival rate"
  short_description: "Apply Order arrival rate with evidence and caveats"
  default_prompt: "Use $tkl-order-arrival-rate to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
