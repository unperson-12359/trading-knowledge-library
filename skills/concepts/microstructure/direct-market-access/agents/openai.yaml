interface:
  display_name: "Direct market access"
  short_description: "Apply Direct market access with evidence and caveats"
  default_prompt: "Use $tkl-direct-market-access to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
