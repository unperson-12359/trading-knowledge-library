interface:
  display_name: "Ask"
  short_description: "Apply Ask with evidence and caveats"
  default_prompt: "Use $tkl-ask to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
