interface:
  display_name: "Layering"
  short_description: "Apply Layering with evidence and caveats"
  default_prompt: "Use $tkl-layering to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
