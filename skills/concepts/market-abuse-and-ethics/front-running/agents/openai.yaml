interface:
  display_name: "Front running"
  short_description: "Apply Front running with evidence and caveats"
  default_prompt: "Use $tkl-front-running to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
