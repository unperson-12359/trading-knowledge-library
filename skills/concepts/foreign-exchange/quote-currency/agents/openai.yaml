interface:
  display_name: "Quote currency"
  short_description: "Apply Quote currency with evidence and caveats"
  default_prompt: "Use $tkl-quote-currency to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
