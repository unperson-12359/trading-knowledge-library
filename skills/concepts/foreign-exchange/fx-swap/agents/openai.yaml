interface:
  display_name: "FX swap"
  short_description: "Apply FX swap with evidence and caveats"
  default_prompt: "Use $tkl-fx-swap to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
