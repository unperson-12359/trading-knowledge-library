interface:
  display_name: "Dollar index"
  short_description: "Apply Dollar index with evidence and caveats"
  default_prompt: "Use $tkl-dollar-index to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
