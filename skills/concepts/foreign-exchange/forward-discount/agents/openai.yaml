interface:
  display_name: "Forward discount"
  short_description: "Apply Forward discount with evidence and caveats"
  default_prompt: "Use $tkl-forward-discount to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
