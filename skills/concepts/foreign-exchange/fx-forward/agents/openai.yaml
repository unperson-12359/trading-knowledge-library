interface:
  display_name: "FX forward"
  short_description: "Apply FX forward with evidence and caveats"
  default_prompt: "Use $tkl-fx-forward to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
