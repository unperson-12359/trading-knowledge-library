interface:
  display_name: "Black-Scholes model"
  short_description: "Apply Black-Scholes model with evidence and caveats"
  default_prompt: "Use $tkl-black-scholes-model to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
