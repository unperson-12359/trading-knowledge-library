interface:
  display_name: "Implied move"
  short_description: "Apply Implied move with evidence and caveats"
  default_prompt: "Use $tkl-implied-move to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
