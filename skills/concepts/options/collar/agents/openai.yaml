interface:
  display_name: "Collar"
  short_description: "Apply Collar with evidence and caveats"
  default_prompt: "Use $tkl-collar to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
