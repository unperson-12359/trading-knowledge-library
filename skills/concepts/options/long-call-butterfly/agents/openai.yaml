interface:
  display_name: "Long call butterfly"
  short_description: "Apply Long call butterfly with evidence and caveats"
  default_prompt: "Use $tkl-long-call-butterfly to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
