interface:
  display_name: "Option series"
  short_description: "Apply Option series with evidence and caveats"
  default_prompt: "Use $tkl-option-series to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
