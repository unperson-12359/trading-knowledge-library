interface:
  display_name: "Binomial option model"
  short_description: "Apply Binomial option model with evidence and caveats"
  default_prompt: "Use $tkl-binomial-option-model to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
