interface:
  display_name: "Option chain"
  short_description: "Apply Option chain with evidence and caveats"
  default_prompt: "Use $tkl-option-chain to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
