interface:
  display_name: "Short strangle"
  short_description: "Apply Short strangle with evidence and caveats"
  default_prompt: "Use $tkl-short-strangle to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
