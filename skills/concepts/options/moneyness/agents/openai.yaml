interface:
  display_name: "Moneyness"
  short_description: "Apply Moneyness with evidence and caveats"
  default_prompt: "Use $tkl-moneyness to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
