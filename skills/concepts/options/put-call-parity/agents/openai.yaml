interface:
  display_name: "Put-call parity"
  short_description: "Apply Put-call parity with evidence and caveats"
  default_prompt: "Use $tkl-put-call-parity to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
