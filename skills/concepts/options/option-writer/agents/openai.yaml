interface:
  display_name: "Option writer"
  short_description: "Apply Option writer with evidence and caveats"
  default_prompt: "Use $tkl-option-writer to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
