interface:
  display_name: "Long straddle"
  short_description: "Apply Long straddle with evidence and caveats"
  default_prompt: "Use $tkl-long-straddle to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
