interface:
  display_name: "Monte Carlo option pricing"
  short_description: "Apply Monte Carlo option pricing with evidence and caveats"
  default_prompt: "Use $tkl-monte-carlo-option-pricing to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
