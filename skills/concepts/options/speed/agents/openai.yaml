interface:
  display_name: "Speed"
  short_description: "Apply Speed with evidence and caveats"
  default_prompt: "Use $tkl-speed to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
