interface:
  display_name: "Backspread"
  short_description: "Apply Backspread with evidence and caveats"
  default_prompt: "Use $tkl-backspread to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
