interface:
  display_name: "Risk reversal"
  short_description: "Apply Risk reversal with evidence and caveats"
  default_prompt: "Use $tkl-risk-reversal to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
