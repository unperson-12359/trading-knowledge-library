interface:
  display_name: "Long strangle"
  short_description: "Apply Long strangle with evidence and caveats"
  default_prompt: "Use $tkl-long-strangle to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
