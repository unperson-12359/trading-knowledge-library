interface:
  display_name: "Color"
  short_description: "Apply Color with evidence and caveats"
  default_prompt: "Use $tkl-color to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
