interface:
  display_name: "Max pain"
  short_description: "Apply Max pain with evidence and caveats"
  default_prompt: "Use $tkl-max-pain to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
