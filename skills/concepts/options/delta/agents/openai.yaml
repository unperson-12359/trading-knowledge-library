interface:
  display_name: "Delta"
  short_description: "Apply Delta with evidence and caveats"
  default_prompt: "Use $tkl-delta to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
