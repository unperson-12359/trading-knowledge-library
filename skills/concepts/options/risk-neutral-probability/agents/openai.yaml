interface:
  display_name: "Risk-neutral probability"
  short_description: "Apply Risk-neutral probability with evidence and caveats"
  default_prompt: "Use $tkl-risk-neutral-probability to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
