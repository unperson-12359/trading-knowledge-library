interface:
  display_name: "Naked call"
  short_description: "Apply Naked call with evidence and caveats"
  default_prompt: "Use $tkl-naked-call to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
