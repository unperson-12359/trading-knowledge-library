interface:
  display_name: "Covered call"
  short_description: "Apply Covered call with evidence and caveats"
  default_prompt: "Use $tkl-covered-call to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
