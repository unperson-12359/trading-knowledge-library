interface:
  display_name: "Vega"
  short_description: "Apply Vega with evidence and caveats"
  default_prompt: "Use $tkl-vega to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
