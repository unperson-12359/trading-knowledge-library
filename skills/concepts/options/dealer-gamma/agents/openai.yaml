interface:
  display_name: "Dealer gamma"
  short_description: "Apply Dealer gamma with evidence and caveats"
  default_prompt: "Use $tkl-dealer-gamma to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
