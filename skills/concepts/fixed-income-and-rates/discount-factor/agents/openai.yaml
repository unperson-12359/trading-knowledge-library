interface:
  display_name: "Discount factor"
  short_description: "Apply Discount factor with evidence and caveats"
  default_prompt: "Use $tkl-discount-factor to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
