interface:
  display_name: "Spot rate curve"
  short_description: "Apply Spot rate curve with evidence and caveats"
  default_prompt: "Use $tkl-spot-rate-curve to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
