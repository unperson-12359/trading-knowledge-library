interface:
  display_name: "Inflation swap"
  short_description: "Apply Inflation swap with evidence and caveats"
  default_prompt: "Use $tkl-inflation-swap to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
