interface:
  display_name: "Negative convexity"
  short_description: "Apply Negative convexity with evidence and caveats"
  default_prompt: "Use $tkl-negative-convexity to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
