interface:
  display_name: "Swap spread"
  short_description: "Apply Swap spread with evidence and caveats"
  default_prompt: "Use $tkl-swap-spread to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
