interface:
  display_name: "Yield curve"
  short_description: "Apply Yield curve with evidence and caveats"
  default_prompt: "Use $tkl-yield-curve to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
