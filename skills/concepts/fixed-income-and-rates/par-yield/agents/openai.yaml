interface:
  display_name: "Par yield"
  short_description: "Apply Par yield with evidence and caveats"
  default_prompt: "Use $tkl-par-yield to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
