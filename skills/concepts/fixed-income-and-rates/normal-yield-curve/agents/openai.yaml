interface:
  display_name: "Normal yield curve"
  short_description: "Apply Normal yield curve with evidence and caveats"
  default_prompt: "Use $tkl-normal-yield-curve to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
