interface:
  display_name: "Repo rate"
  short_description: "Apply Repo rate with evidence and caveats"
  default_prompt: "Use $tkl-repo-rate to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
