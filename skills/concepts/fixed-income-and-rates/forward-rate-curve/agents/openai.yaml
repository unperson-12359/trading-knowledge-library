interface:
  display_name: "Forward rate curve"
  short_description: "Apply Forward rate curve with evidence and caveats"
  default_prompt: "Use $tkl-forward-rate-curve to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
