interface:
  display_name: "Inverted yield curve"
  short_description: "Apply Inverted yield curve with evidence and caveats"
  default_prompt: "Use $tkl-inverted-yield-curve to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
