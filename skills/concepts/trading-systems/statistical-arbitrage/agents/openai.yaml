interface:
  display_name: "Statistical arbitrage"
  short_description: "Apply Statistical arbitrage with evidence and caveats"
  default_prompt: "Use $tkl-statistical-arbitrage to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
