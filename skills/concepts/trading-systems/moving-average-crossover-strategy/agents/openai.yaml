interface:
  display_name: "Moving-average crossover strategy"
  short_description: "Apply Moving-average crossover strategy with evidence"
  default_prompt: "Use $tkl-moving-average-crossover-strategy to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
