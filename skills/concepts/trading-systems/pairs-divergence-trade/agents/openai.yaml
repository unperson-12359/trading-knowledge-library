interface:
  display_name: "Pairs divergence trade"
  short_description: "Apply Pairs divergence trade with evidence and caveats"
  default_prompt: "Use $tkl-pairs-divergence-trade to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
