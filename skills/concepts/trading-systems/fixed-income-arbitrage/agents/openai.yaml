interface:
  display_name: "Fixed-income arbitrage"
  short_description: "Apply Fixed-income arbitrage with evidence and caveats"
  default_prompt: "Use $tkl-fixed-income-arbitrage to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
