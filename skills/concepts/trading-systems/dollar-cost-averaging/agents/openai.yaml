interface:
  display_name: "Dollar-cost averaging"
  short_description: "Apply Dollar-cost averaging with evidence and caveats"
  default_prompt: "Use $tkl-dollar-cost-averaging to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
