interface:
  display_name: "Index arbitrage"
  short_description: "Apply Index arbitrage with evidence and caveats"
  default_prompt: "Use $tkl-index-arbitrage to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
