interface:
  display_name: "Latency arbitrage"
  short_description: "Apply Latency arbitrage with evidence and caveats"
  default_prompt: "Use $tkl-latency-arbitrage to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
