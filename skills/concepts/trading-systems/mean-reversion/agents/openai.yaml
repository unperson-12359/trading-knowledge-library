interface:
  display_name: "Mean reversion"
  short_description: "Apply Mean reversion with evidence and caveats"
  default_prompt: "Use $tkl-mean-reversion to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
