interface:
  display_name: "Value averaging"
  short_description: "Apply Value averaging with evidence and caveats"
  default_prompt: "Use $tkl-value-averaging to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
