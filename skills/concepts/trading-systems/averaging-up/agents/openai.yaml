interface:
  display_name: "Averaging up"
  short_description: "Apply Averaging up with evidence and caveats"
  default_prompt: "Use $tkl-averaging-up to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
