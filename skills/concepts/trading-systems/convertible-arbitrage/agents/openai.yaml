interface:
  display_name: "Convertible arbitrage"
  short_description: "Apply Convertible arbitrage with evidence and caveats"
  default_prompt: "Use $tkl-convertible-arbitrage to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
