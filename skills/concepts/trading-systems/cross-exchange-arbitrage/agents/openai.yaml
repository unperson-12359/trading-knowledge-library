interface:
  display_name: "Cross-exchange arbitrage"
  short_description: "Apply Cross-exchange arbitrage with evidence and caveats"
  default_prompt: "Use $tkl-cross-exchange-arbitrage to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
