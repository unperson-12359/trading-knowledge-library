interface:
  display_name: "Gamma scalping"
  short_description: "Apply Gamma scalping with evidence and caveats"
  default_prompt: "Use $tkl-gamma-scalping to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
