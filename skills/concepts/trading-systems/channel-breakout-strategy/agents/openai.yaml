interface:
  display_name: "Channel breakout strategy"
  short_description: "Apply Channel breakout strategy with evidence and caveats"
  default_prompt: "Use $tkl-channel-breakout-strategy to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
