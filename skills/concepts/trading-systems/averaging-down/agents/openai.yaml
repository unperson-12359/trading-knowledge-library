interface:
  display_name: "Averaging down"
  short_description: "Apply Averaging down with evidence and caveats"
  default_prompt: "Use $tkl-averaging-down to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
