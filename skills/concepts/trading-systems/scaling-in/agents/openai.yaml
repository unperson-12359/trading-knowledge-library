interface:
  display_name: "Scaling in"
  short_description: "Apply Scaling in with evidence and caveats"
  default_prompt: "Use $tkl-scaling-in to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
