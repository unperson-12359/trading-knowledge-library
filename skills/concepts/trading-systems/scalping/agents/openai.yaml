interface:
  display_name: "Scalping"
  short_description: "Apply Scalping with evidence and caveats"
  default_prompt: "Use $tkl-scalping to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
