interface:
  display_name: "Pairs convergence trade"
  short_description: "Apply Pairs convergence trade with evidence and caveats"
  default_prompt: "Use $tkl-pairs-convergence-trade to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
