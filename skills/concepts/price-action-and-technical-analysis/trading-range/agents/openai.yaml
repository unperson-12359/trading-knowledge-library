interface:
  display_name: "Trading range"
  short_description: "Apply Trading range with evidence and caveats"
  default_prompt: "Use $tkl-trading-range to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
