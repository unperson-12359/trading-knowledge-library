interface:
  display_name: "Pullback"
  short_description: "Apply Pullback with evidence and caveats"
  default_prompt: "Use $tkl-pullback to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
