interface:
  display_name: "Discount zone"
  short_description: "Apply Discount zone with evidence and caveats"
  default_prompt: "Use $tkl-discount-zone to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
