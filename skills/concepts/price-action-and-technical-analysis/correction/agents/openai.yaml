interface:
  display_name: "Correction"
  short_description: "Apply Correction with evidence and caveats"
  default_prompt: "Use $tkl-correction to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
