interface:
  display_name: "Retest"
  short_description: "Apply Retest with evidence and caveats"
  default_prompt: "Use $tkl-retest to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
