interface:
  display_name: "Continuation"
  short_description: "Apply Continuation with evidence and caveats"
  default_prompt: "Use $tkl-continuation to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
