interface:
  display_name: "Lower low"
  short_description: "Apply Lower low with evidence and caveats"
  default_prompt: "Use $tkl-lower-low to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
