interface:
  display_name: "Channel"
  short_description: "Apply Channel with evidence and caveats"
  default_prompt: "Use $tkl-channel to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
