interface:
  display_name: "Breakdown"
  short_description: "Apply Breakdown with evidence and caveats"
  default_prompt: "Use $tkl-breakdown to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
