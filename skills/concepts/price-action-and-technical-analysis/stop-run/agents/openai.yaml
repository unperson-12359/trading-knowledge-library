interface:
  display_name: "Stop run"
  short_description: "Apply Stop run with evidence and caveats"
  default_prompt: "Use $tkl-stop-run to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
