interface:
  display_name: "Monthly pivot"
  short_description: "Apply Monthly pivot with evidence and caveats"
  default_prompt: "Use $tkl-monthly-pivot to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
