interface:
  display_name: "Uptrend"
  short_description: "Apply Uptrend with evidence and caveats"
  default_prompt: "Use $tkl-uptrend to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
