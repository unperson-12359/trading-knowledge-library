interface:
  display_name: "Ascending channel"
  short_description: "Apply Ascending channel with evidence and caveats"
  default_prompt: "Use $tkl-ascending-channel to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
