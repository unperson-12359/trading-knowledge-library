interface:
  display_name: "Change of character"
  short_description: "Apply Change of character with evidence and caveats"
  default_prompt: "Use $tkl-change-of-character to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
