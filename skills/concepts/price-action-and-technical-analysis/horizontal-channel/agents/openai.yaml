interface:
  display_name: "Horizontal channel"
  short_description: "Apply Horizontal channel with evidence and caveats"
  default_prompt: "Use $tkl-horizontal-channel to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
