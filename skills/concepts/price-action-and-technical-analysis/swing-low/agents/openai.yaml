interface:
  display_name: "Swing low"
  short_description: "Apply Swing low with evidence and caveats"
  default_prompt: "Use $tkl-swing-low to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
