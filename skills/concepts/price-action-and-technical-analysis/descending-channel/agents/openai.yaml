interface:
  display_name: "Descending channel"
  short_description: "Apply Descending channel with evidence and caveats"
  default_prompt: "Use $tkl-descending-channel to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
