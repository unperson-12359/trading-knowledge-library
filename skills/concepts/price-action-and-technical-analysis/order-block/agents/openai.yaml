interface:
  display_name: "Order block"
  short_description: "Apply Order block with evidence and caveats"
  default_prompt: "Use $tkl-order-block to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
