interface:
  display_name: "Higher low"
  short_description: "Apply Higher low with evidence and caveats"
  default_prompt: "Use $tkl-higher-low to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
