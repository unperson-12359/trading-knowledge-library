interface:
  display_name: "Compression"
  short_description: "Apply Compression with evidence and caveats"
  default_prompt: "Use $tkl-compression to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
