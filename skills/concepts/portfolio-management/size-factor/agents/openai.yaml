interface:
  display_name: "Size factor"
  short_description: "Apply Size factor with evidence and caveats"
  default_prompt: "Use $tkl-size-factor to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
