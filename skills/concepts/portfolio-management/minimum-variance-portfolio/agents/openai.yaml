interface:
  display_name: "Minimum-variance portfolio"
  short_description: "Apply Minimum-variance portfolio with evidence and caveats"
  default_prompt: "Use $tkl-minimum-variance-portfolio to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
