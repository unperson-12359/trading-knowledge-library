interface:
  display_name: "Duration hedge"
  short_description: "Apply Duration hedge with evidence and caveats"
  default_prompt: "Use $tkl-duration-hedge to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
