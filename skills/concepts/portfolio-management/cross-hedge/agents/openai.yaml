interface:
  display_name: "Cross hedge"
  short_description: "Apply Cross hedge with evidence and caveats"
  default_prompt: "Use $tkl-cross-hedge to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
