interface:
  display_name: "Rebalancing"
  short_description: "Apply Rebalancing with evidence and caveats"
  default_prompt: "Use $tkl-rebalancing to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
