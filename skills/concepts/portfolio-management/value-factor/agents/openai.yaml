interface:
  display_name: "Value factor"
  short_description: "Apply Value factor with evidence and caveats"
  default_prompt: "Use $tkl-value-factor to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
