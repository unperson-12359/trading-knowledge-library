interface:
  display_name: "Term factor"
  short_description: "Apply Term factor with evidence and caveats"
  default_prompt: "Use $tkl-term-factor to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
