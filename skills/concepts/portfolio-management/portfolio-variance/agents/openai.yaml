interface:
  display_name: "Portfolio variance"
  short_description: "Apply Portfolio variance with evidence and caveats"
  default_prompt: "Use $tkl-portfolio-variance to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
