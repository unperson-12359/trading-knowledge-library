interface:
  display_name: "Alpha"
  short_description: "Apply Alpha with evidence and caveats"
  default_prompt: "Use $tkl-alpha to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
