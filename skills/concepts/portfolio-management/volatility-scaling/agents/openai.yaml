interface:
  display_name: "Volatility scaling"
  short_description: "Apply Volatility scaling with evidence and caveats"
  default_prompt: "Use $tkl-volatility-scaling to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
