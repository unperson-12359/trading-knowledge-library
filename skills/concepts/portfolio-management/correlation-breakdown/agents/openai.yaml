interface:
  display_name: "Correlation breakdown"
  short_description: "Apply Correlation breakdown with evidence and caveats"
  default_prompt: "Use $tkl-correlation-breakdown to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
