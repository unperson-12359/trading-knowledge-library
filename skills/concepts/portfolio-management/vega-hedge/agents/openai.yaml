interface:
  display_name: "Vega hedge"
  short_description: "Apply Vega hedge with evidence and caveats"
  default_prompt: "Use $tkl-vega-hedge to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
