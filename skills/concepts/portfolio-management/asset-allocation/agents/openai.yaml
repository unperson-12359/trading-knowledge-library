interface:
  display_name: "Asset allocation"
  short_description: "Apply Asset allocation with evidence and caveats"
  default_prompt: "Use $tkl-asset-allocation to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
