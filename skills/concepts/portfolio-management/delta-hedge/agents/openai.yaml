interface:
  display_name: "Delta hedge"
  short_description: "Apply Delta hedge with evidence and caveats"
  default_prompt: "Use $tkl-delta-hedge to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
