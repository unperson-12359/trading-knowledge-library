interface:
  display_name: "Factor model"
  short_description: "Apply Factor model with evidence and caveats"
  default_prompt: "Use $tkl-factor-model to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
