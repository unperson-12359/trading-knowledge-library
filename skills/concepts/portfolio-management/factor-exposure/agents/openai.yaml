interface:
  display_name: "Factor exposure"
  short_description: "Apply Factor exposure with evidence and caveats"
  default_prompt: "Use $tkl-factor-exposure to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
