interface:
  display_name: "Composite operator"
  short_description: "Apply Composite operator with evidence and caveats"
  default_prompt: "Use $tkl-composite-operator to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
