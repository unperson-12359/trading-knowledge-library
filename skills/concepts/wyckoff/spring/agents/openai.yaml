interface:
  display_name: "Spring"
  short_description: "Apply Spring with evidence and caveats"
  default_prompt: "Use $tkl-spring to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
