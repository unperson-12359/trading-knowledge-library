interface:
  display_name: "Upthrust"
  short_description: "Apply Upthrust with evidence and caveats"
  default_prompt: "Use $tkl-upthrust to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
