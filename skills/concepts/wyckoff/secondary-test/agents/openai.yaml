interface:
  display_name: "Secondary test"
  short_description: "Apply Secondary test with evidence and caveats"
  default_prompt: "Use $tkl-secondary-test to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
