interface:
  display_name: "Terminal shakeout"
  short_description: "Apply Terminal shakeout with evidence and caveats"
  default_prompt: "Use $tkl-terminal-shakeout to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
