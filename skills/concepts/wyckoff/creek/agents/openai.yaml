interface:
  display_name: "Creek"
  short_description: "Apply Creek with evidence and caveats"
  default_prompt: "Use $tkl-creek to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
