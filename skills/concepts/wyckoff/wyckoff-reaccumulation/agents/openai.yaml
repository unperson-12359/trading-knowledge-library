interface:
  display_name: "Wyckoff reaccumulation"
  short_description: "Apply Wyckoff reaccumulation with evidence and caveats"
  default_prompt: "Use $tkl-wyckoff-reaccumulation to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
