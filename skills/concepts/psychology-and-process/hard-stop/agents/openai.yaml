interface:
  display_name: "Hard stop"
  short_description: "Apply Hard stop with evidence and caveats"
  default_prompt: "Use $tkl-hard-stop to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
