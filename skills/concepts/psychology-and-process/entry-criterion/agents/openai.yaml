interface:
  display_name: "Entry criterion"
  short_description: "Apply Entry criterion with evidence and caveats"
  default_prompt: "Use $tkl-entry-criterion to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
