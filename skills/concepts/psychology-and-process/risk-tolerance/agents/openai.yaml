interface:
  display_name: "Risk tolerance"
  short_description: "Apply Risk tolerance with evidence and caveats"
  default_prompt: "Use $tkl-risk-tolerance to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
