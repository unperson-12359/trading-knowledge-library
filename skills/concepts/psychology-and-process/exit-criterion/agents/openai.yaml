interface:
  display_name: "Exit criterion"
  short_description: "Apply Exit criterion with evidence and caveats"
  default_prompt: "Use $tkl-exit-criterion to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
