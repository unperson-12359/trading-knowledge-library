interface:
  display_name: "Setup quality"
  short_description: "Apply Setup quality with evidence and caveats"
  default_prompt: "Use $tkl-setup-quality to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
