interface:
  display_name: "Decision journal"
  short_description: "Apply Decision journal with evidence and caveats"
  default_prompt: "Use $tkl-decision-journal to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
