interface:
  display_name: "Playbook"
  short_description: "Apply Playbook with evidence and caveats"
  default_prompt: "Use $tkl-playbook to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
