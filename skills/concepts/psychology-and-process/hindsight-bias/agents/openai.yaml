interface:
  display_name: "Hindsight bias"
  short_description: "Apply Hindsight bias with evidence and caveats"
  default_prompt: "Use $tkl-hindsight-bias to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
