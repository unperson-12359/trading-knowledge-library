interface:
  display_name: "Availability heuristic"
  short_description: "Apply Availability heuristic with evidence and caveats"
  default_prompt: "Use $tkl-availability-heuristic to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
