interface:
  display_name: "Process metric"
  short_description: "Apply Process metric with evidence and caveats"
  default_prompt: "Use $tkl-process-metric to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
