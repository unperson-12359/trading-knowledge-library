interface:
  display_name: "Percentile"
  short_description: "Apply Percentile with evidence and caveats"
  default_prompt: "Use $tkl-percentile to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
