interface:
  display_name: "Mode"
  short_description: "Apply Mode with evidence and caveats"
  default_prompt: "Use $tkl-mode to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
