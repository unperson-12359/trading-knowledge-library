interface:
  display_name: "Standard error"
  short_description: "Apply Standard error with evidence and caveats"
  default_prompt: "Use $tkl-standard-error to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
