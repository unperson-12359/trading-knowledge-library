interface:
  display_name: "Variance"
  short_description: "Apply Variance with evidence and caveats"
  default_prompt: "Use $tkl-variance to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
