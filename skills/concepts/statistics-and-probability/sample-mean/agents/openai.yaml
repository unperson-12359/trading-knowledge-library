interface:
  display_name: "Sample mean"
  short_description: "Apply Sample mean with evidence and caveats"
  default_prompt: "Use $tkl-sample-mean to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
