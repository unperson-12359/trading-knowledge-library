interface:
  display_name: "Simple return"
  short_description: "Apply Simple return with evidence and caveats"
  default_prompt: "Use $tkl-simple-return to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
