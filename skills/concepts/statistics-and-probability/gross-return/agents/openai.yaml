interface:
  display_name: "Gross return"
  short_description: "Apply Gross return with evidence and caveats"
  default_prompt: "Use $tkl-gross-return to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
