interface:
  display_name: "Log return"
  short_description: "Apply Log return with evidence and caveats"
  default_prompt: "Use $tkl-log-return to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
