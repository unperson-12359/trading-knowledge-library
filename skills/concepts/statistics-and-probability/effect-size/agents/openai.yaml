interface:
  display_name: "Effect size"
  short_description: "Apply Effect size with evidence and caveats"
  default_prompt: "Use $tkl-effect-size to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
