interface:
  display_name: "Estimator bias"
  short_description: "Apply Estimator bias with evidence and caveats"
  default_prompt: "Use $tkl-estimator-bias to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
