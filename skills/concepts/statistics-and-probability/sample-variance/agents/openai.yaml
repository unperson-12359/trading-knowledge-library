interface:
  display_name: "Sample variance"
  short_description: "Apply Sample variance with evidence and caveats"
  default_prompt: "Use $tkl-sample-variance to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
