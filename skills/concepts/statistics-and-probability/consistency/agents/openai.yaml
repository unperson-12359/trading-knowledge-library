interface:
  display_name: "Consistency"
  short_description: "Apply Consistency with evidence and caveats"
  default_prompt: "Use $tkl-consistency to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
