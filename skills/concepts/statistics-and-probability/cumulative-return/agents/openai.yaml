interface:
  display_name: "Cumulative return"
  short_description: "Apply Cumulative return with evidence and caveats"
  default_prompt: "Use $tkl-cumulative-return to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
