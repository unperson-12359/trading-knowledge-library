interface:
  display_name: "Spearman rank correlation"
  short_description: "Apply Spearman rank correlation with evidence and caveats"
  default_prompt: "Use $tkl-spearman-rank-correlation to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
