interface:
  display_name: "Falling wedge"
  short_description: "Apply Falling wedge with evidence and caveats"
  default_prompt: "Use $tkl-falling-wedge to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
