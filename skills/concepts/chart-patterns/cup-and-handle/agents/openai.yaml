interface:
  display_name: "Cup and handle"
  short_description: "Apply Cup and handle with evidence and caveats"
  default_prompt: "Use $tkl-cup-and-handle to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
