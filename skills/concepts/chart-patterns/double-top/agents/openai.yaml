interface:
  display_name: "Double top"
  short_description: "Apply Double top with evidence and caveats"
  default_prompt: "Use $tkl-double-top to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
