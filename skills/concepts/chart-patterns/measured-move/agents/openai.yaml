interface:
  display_name: "Measured move"
  short_description: "Apply Measured move with evidence and caveats"
  default_prompt: "Use $tkl-measured-move to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
