interface:
  display_name: "Double bottom"
  short_description: "Apply Double bottom with evidence and caveats"
  default_prompt: "Use $tkl-double-bottom to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
