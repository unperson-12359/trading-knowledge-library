interface:
  display_name: "Rounding top"
  short_description: "Apply Rounding top with evidence and caveats"
  default_prompt: "Use $tkl-rounding-top to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
