interface:
  display_name: "Bear flag"
  short_description: "Apply Bear flag with evidence and caveats"
  default_prompt: "Use $tkl-bear-flag to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
