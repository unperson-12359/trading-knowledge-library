interface:
  display_name: "Ascending triangle"
  short_description: "Apply Ascending triangle with evidence and caveats"
  default_prompt: "Use $tkl-ascending-triangle to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
