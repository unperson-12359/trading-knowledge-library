interface:
  display_name: "Descending triangle"
  short_description: "Apply Descending triangle with evidence and caveats"
  default_prompt: "Use $tkl-descending-triangle to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
