interface:
  display_name: "Rounding bottom"
  short_description: "Apply Rounding bottom with evidence and caveats"
  default_prompt: "Use $tkl-rounding-bottom to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
