interface:
  display_name: "Clean price"
  short_description: "Apply Clean price with evidence and caveats"
  default_prompt: "Use $tkl-clean-price to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
