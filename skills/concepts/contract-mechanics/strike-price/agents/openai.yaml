interface:
  display_name: "Strike price"
  short_description: "Apply Strike price with evidence and caveats"
  default_prompt: "Use $tkl-strike-price to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
