interface:
  display_name: "Duration"
  short_description: "Apply Duration with evidence and caveats"
  default_prompt: "Use $tkl-duration to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
