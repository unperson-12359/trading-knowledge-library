interface:
  display_name: "Contract multiplier"
  short_description: "Apply Contract multiplier with evidence and caveats"
  default_prompt: "Use $tkl-contract-multiplier to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
