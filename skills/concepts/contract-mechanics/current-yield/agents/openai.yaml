interface:
  display_name: "Current yield"
  short_description: "Apply Current yield with evidence and caveats"
  default_prompt: "Use $tkl-current-yield to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
