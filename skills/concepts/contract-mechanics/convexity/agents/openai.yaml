interface:
  display_name: "Convexity"
  short_description: "Apply Convexity with evidence and caveats"
  default_prompt: "Use $tkl-convexity to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
