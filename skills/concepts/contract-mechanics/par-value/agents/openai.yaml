interface:
  display_name: "Par value"
  short_description: "Apply Par value with evidence and caveats"
  default_prompt: "Use $tkl-par-value to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
