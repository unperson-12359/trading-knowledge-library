interface:
  display_name: "Tick size"
  short_description: "Apply Tick size with evidence and caveats"
  default_prompt: "Use $tkl-tick-size to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
