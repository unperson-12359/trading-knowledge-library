interface:
  display_name: "Accrued interest"
  short_description: "Apply Accrued interest with evidence and caveats"
  default_prompt: "Use $tkl-accrued-interest to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
