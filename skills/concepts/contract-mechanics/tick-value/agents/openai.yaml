interface:
  display_name: "Tick value"
  short_description: "Apply Tick value with evidence and caveats"
  default_prompt: "Use $tkl-tick-value to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
