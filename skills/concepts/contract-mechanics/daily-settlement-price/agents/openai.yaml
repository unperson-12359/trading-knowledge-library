interface:
  display_name: "Daily settlement price"
  short_description: "Apply Daily settlement price with evidence and caveats"
  default_prompt: "Use $tkl-daily-settlement-price to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
