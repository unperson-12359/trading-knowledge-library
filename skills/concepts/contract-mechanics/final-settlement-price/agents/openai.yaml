interface:
  display_name: "Final settlement price"
  short_description: "Apply Final settlement price with evidence and caveats"
  default_prompt: "Use $tkl-final-settlement-price to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
