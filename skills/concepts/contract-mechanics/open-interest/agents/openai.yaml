interface:
  display_name: "Open interest"
  short_description: "Apply Open interest with evidence and caveats"
  default_prompt: "Use $tkl-open-interest to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
