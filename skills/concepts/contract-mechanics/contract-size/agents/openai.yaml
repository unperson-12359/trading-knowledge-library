interface:
  display_name: "Contract size"
  short_description: "Apply Contract size with evidence and caveats"
  default_prompt: "Use $tkl-contract-size to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
