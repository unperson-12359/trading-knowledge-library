interface:
  display_name: "Three inside up"
  short_description: "Apply Three inside up with evidence and caveats"
  default_prompt: "Use $tkl-three-inside-up to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
