interface:
  display_name: "Pin bar"
  short_description: "Apply Pin bar with evidence and caveats"
  default_prompt: "Use $tkl-pin-bar to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
