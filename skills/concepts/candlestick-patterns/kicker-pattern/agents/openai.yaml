interface:
  display_name: "Kicker pattern"
  short_description: "Apply Kicker pattern with evidence and caveats"
  default_prompt: "Use $tkl-kicker-pattern to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
