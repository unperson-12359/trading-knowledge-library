interface:
  display_name: "Hammer"
  short_description: "Apply Hammer with evidence and caveats"
  default_prompt: "Use $tkl-hammer to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
