interface:
  display_name: "Long-legged doji"
  short_description: "Apply Long-legged doji with evidence and caveats"
  default_prompt: "Use $tkl-long-legged-doji to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
