interface:
  display_name: "Three inside down"
  short_description: "Apply Three inside down with evidence and caveats"
  default_prompt: "Use $tkl-three-inside-down to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
