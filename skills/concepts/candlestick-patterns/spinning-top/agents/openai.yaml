interface:
  display_name: "Spinning top"
  short_description: "Apply Spinning top with evidence and caveats"
  default_prompt: "Use $tkl-spinning-top to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
