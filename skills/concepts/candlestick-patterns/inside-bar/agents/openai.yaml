interface:
  display_name: "Inside bar"
  short_description: "Apply Inside bar with evidence and caveats"
  default_prompt: "Use $tkl-inside-bar to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
