interface:
  display_name: "Dragonfly doji"
  short_description: "Apply Dragonfly doji with evidence and caveats"
  default_prompt: "Use $tkl-dragonfly-doji to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
