interface:
  display_name: "Doji"
  short_description: "Apply Doji with evidence and caveats"
  default_prompt: "Use $tkl-doji to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
