interface:
  display_name: "Stock split"
  short_description: "Apply Stock split with evidence and caveats"
  default_prompt: "Use $tkl-stock-split to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
