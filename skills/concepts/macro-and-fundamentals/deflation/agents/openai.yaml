interface:
  display_name: "Deflation"
  short_description: "Apply Deflation with evidence and caveats"
  default_prompt: "Use $tkl-deflation to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
