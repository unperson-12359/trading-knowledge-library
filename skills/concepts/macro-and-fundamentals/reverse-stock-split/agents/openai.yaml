interface:
  display_name: "Reverse stock split"
  short_description: "Apply Reverse stock split with evidence and caveats"
  default_prompt: "Use $tkl-reverse-stock-split to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
