interface:
  display_name: "Fiscal policy"
  short_description: "Apply Fiscal policy with evidence and caveats"
  default_prompt: "Use $tkl-fiscal-policy to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
