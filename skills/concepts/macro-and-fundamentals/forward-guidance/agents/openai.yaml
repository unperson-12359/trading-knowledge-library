interface:
  display_name: "Forward guidance"
  short_description: "Apply Forward guidance with evidence and caveats"
  default_prompt: "Use $tkl-forward-guidance to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
