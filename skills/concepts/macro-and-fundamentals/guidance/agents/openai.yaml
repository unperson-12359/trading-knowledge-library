interface:
  display_name: "Guidance"
  short_description: "Apply Guidance with evidence and caveats"
  default_prompt: "Use $tkl-guidance to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
