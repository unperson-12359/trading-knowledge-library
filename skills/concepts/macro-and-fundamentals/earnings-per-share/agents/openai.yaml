interface:
  display_name: "Earnings per share"
  short_description: "Apply Earnings per share with evidence and caveats"
  default_prompt: "Use $tkl-earnings-per-share to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
