interface:
  display_name: "Record date"
  short_description: "Apply Record date with evidence and caveats"
  default_prompt: "Use $tkl-record-date to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
