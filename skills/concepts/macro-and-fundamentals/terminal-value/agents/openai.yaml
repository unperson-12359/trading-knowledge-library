interface:
  display_name: "Terminal value"
  short_description: "Apply Terminal value with evidence and caveats"
  default_prompt: "Use $tkl-terminal-value to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
