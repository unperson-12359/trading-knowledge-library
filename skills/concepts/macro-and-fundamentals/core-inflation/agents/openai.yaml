interface:
  display_name: "Core inflation"
  short_description: "Apply Core inflation with evidence and caveats"
  default_prompt: "Use $tkl-core-inflation to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
