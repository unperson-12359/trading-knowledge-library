interface:
  display_name: "Consumer price index"
  short_description: "Apply Consumer price index with evidence and caveats"
  default_prompt: "Use $tkl-consumer-price-index to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
