interface:
  display_name: "Producer price index"
  short_description: "Apply Producer price index with evidence and caveats"
  default_prompt: "Use $tkl-producer-price-index to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
