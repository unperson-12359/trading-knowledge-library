interface:
  display_name: "Dividend yield"
  short_description: "Apply Dividend yield with evidence and caveats"
  default_prompt: "Use $tkl-dividend-yield to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
