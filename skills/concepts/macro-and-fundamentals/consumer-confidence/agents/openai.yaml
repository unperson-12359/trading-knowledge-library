interface:
  display_name: "Consumer confidence"
  short_description: "Apply Consumer confidence with evidence and caveats"
  default_prompt: "Use $tkl-consumer-confidence to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
