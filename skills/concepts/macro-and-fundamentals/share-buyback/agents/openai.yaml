interface:
  display_name: "Share buyback"
  short_description: "Apply Share buyback with evidence and caveats"
  default_prompt: "Use $tkl-share-buyback to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
