interface:
  display_name: "Real GDP"
  short_description: "Apply Real GDP with evidence and caveats"
  default_prompt: "Use $tkl-real-gdp to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
