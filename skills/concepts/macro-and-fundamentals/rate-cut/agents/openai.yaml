interface:
  display_name: "Rate cut"
  short_description: "Apply Rate cut with evidence and caveats"
  default_prompt: "Use $tkl-rate-cut to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
