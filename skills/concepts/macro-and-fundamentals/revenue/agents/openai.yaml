interface:
  display_name: "Revenue"
  short_description: "Apply Revenue with evidence and caveats"
  default_prompt: "Use $tkl-revenue to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
