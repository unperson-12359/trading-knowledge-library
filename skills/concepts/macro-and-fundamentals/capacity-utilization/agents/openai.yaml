interface:
  display_name: "Capacity utilization"
  short_description: "Apply Capacity utilization with evidence and caveats"
  default_prompt: "Use $tkl-capacity-utilization to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
