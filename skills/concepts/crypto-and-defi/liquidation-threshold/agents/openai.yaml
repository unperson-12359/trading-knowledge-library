interface:
  display_name: "Liquidation threshold"
  short_description: "Apply Liquidation threshold with evidence and caveats"
  default_prompt: "Use $tkl-liquidation-threshold to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
