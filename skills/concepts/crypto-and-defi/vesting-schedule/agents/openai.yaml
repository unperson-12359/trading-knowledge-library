interface:
  display_name: "Vesting schedule"
  short_description: "Apply Vesting schedule with evidence and caveats"
  default_prompt: "Use $tkl-vesting-schedule to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
