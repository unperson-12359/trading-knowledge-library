interface:
  display_name: "Minting"
  short_description: "Apply Minting with evidence and caveats"
  default_prompt: "Use $tkl-minting to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
