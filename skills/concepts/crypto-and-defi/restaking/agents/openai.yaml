interface:
  display_name: "Restaking"
  short_description: "Apply Restaking with evidence and caveats"
  default_prompt: "Use $tkl-restaking to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
