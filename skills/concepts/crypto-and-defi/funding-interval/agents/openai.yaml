interface:
  display_name: "Funding interval"
  short_description: "Apply Funding interval with evidence and caveats"
  default_prompt: "Use $tkl-funding-interval to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
