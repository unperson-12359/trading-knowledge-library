interface:
  display_name: "Liquidation bonus"
  short_description: "Apply Liquidation bonus with evidence and caveats"
  default_prompt: "Use $tkl-liquidation-bonus to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
