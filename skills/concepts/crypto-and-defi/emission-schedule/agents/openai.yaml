interface:
  display_name: "Emission schedule"
  short_description: "Apply Emission schedule with evidence and caveats"
  default_prompt: "Use $tkl-emission-schedule to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
