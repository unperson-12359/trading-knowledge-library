interface:
  display_name: "Block space"
  short_description: "Apply Block space with evidence and caveats"
  default_prompt: "Use $tkl-block-space to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
