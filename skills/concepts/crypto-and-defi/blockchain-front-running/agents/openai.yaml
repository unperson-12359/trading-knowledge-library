interface:
  display_name: "Blockchain front-running"
  short_description: "Apply Blockchain front-running with evidence and caveats"
  default_prompt: "Use $tkl-blockchain-front-running to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
