interface:
  display_name: "Exchange outflow"
  short_description: "Apply Exchange outflow with evidence and caveats"
  default_prompt: "Use $tkl-exchange-outflow to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
