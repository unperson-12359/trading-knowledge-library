interface:
  display_name: "Lending protocol"
  short_description: "Apply Lending protocol with evidence and caveats"
  default_prompt: "Use $tkl-lending-protocol to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
