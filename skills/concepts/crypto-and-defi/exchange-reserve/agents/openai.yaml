interface:
  display_name: "Exchange reserve"
  short_description: "Apply Exchange reserve with evidence and caveats"
  default_prompt: "Use $tkl-exchange-reserve to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
