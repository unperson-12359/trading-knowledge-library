interface:
  display_name: "Miner"
  short_description: "Apply Miner with evidence and caveats"
  default_prompt: "Use $tkl-miner to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
