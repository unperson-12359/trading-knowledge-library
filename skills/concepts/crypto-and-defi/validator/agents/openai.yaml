interface:
  display_name: "Validator"
  short_description: "Apply Validator with evidence and caveats"
  default_prompt: "Use $tkl-validator to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
