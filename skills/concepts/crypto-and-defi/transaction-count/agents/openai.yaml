interface:
  display_name: "Transaction count"
  short_description: "Apply Transaction count with evidence and caveats"
  default_prompt: "Use $tkl-transaction-count to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
