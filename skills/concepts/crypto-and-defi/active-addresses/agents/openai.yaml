interface:
  display_name: "Active addresses"
  short_description: "Apply Active addresses with evidence and caveats"
  default_prompt: "Use $tkl-active-addresses to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
