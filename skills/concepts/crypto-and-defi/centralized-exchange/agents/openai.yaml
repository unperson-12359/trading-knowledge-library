interface:
  display_name: "Centralized exchange"
  short_description: "Apply Centralized exchange with evidence and caveats"
  default_prompt: "Use $tkl-centralized-exchange to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
