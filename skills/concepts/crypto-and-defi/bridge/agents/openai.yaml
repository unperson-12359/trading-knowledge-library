interface:
  display_name: "Bridge"
  short_description: "Apply Bridge with evidence and caveats"
  default_prompt: "Use $tkl-bridge to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
