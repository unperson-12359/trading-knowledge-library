interface:
  display_name: "Distributed ledger"
  short_description: "Apply Distributed ledger with evidence and caveats"
  default_prompt: "Use $tkl-distributed-ledger to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
