interface:
  display_name: "Open interest leverage ratio"
  short_description: "Apply Open interest leverage ratio with evidence and caveats"
  default_prompt: "Use $tkl-open-interest-leverage-ratio to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
