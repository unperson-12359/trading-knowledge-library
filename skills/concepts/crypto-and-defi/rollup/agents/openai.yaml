interface:
  display_name: "Rollup"
  short_description: "Apply Rollup with evidence and caveats"
  default_prompt: "Use $tkl-rollup to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
