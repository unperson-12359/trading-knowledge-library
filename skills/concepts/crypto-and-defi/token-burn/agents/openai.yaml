interface:
  display_name: "Token burn"
  short_description: "Apply Token burn with evidence and caveats"
  default_prompt: "Use $tkl-token-burn to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
