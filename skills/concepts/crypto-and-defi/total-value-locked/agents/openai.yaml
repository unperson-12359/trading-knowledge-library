interface:
  display_name: "Total value locked"
  short_description: "Apply Total value locked with evidence and caveats"
  default_prompt: "Use $tkl-total-value-locked to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
