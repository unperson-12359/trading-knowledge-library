interface:
  display_name: "Flash loan"
  short_description: "Apply Flash loan with evidence and caveats"
  default_prompt: "Use $tkl-flash-loan to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
