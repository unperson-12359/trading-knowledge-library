interface:
  display_name: "Predicted funding rate"
  short_description: "Apply Predicted funding rate with evidence and caveats"
  default_prompt: "Use $tkl-predicted-funding-rate to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
