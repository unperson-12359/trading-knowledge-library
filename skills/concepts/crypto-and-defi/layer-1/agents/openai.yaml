interface:
  display_name: "Layer 1"
  short_description: "Apply Layer 1 with evidence and caveats"
  default_prompt: "Use $tkl-layer-1 to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
