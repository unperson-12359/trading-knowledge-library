interface:
  display_name: "Public key"
  short_description: "Apply Public key with evidence and caveats"
  default_prompt: "Use $tkl-public-key to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
