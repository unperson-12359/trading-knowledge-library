interface:
  display_name: "Consensus mechanism"
  short_description: "Apply Consensus mechanism with evidence and caveats"
  default_prompt: "Use $tkl-consensus-mechanism to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
