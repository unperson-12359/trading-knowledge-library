interface:
  display_name: "Private key"
  short_description: "Apply Private key with evidence and caveats"
  default_prompt: "Use $tkl-private-key to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
