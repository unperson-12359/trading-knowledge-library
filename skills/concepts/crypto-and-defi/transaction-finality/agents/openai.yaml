interface:
  display_name: "Transaction finality"
  short_description: "Apply Transaction finality with evidence and caveats"
  default_prompt: "Use $tkl-transaction-finality to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
