interface:
  display_name: "Node"
  short_description: "Apply Node with evidence and caveats"
  default_prompt: "Use $tkl-node to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
