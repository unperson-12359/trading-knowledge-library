interface:
  display_name: "Staking"
  short_description: "Apply Staking with evidence and caveats"
  default_prompt: "Use $tkl-staking to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
