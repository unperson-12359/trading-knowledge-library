interface:
  display_name: "Funding rate"
  short_description: "Apply Funding rate with evidence and caveats"
  default_prompt: "Use $tkl-funding-rate to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
