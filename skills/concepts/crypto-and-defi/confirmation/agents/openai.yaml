interface:
  display_name: "Confirmation"
  short_description: "Apply Confirmation with evidence and caveats"
  default_prompt: "Use $tkl-confirmation to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
