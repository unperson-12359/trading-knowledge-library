interface:
  display_name: "Price impact in AMMs"
  short_description: "Apply Price impact in AMMs with evidence and caveats"
  default_prompt: "Use $tkl-price-impact-in-amms to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
