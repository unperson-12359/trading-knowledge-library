interface:
  display_name: "Layer 2"
  short_description: "Apply Layer 2 with evidence and caveats"
  default_prompt: "Use $tkl-layer-2 to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
