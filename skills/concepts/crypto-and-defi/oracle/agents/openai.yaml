interface:
  display_name: "Oracle"
  short_description: "Apply Oracle with evidence and caveats"
  default_prompt: "Use $tkl-oracle to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
