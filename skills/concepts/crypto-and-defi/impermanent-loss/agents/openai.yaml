interface:
  display_name: "Impermanent loss"
  short_description: "Apply Impermanent loss with evidence and caveats"
  default_prompt: "Use $tkl-impermanent-loss to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
