interface:
  display_name: "Staking ratio"
  short_description: "Apply Staking ratio with evidence and caveats"
  default_prompt: "Use $tkl-staking-ratio to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
