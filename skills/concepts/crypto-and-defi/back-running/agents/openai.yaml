interface:
  display_name: "Back-running"
  short_description: "Apply Back-running with evidence and caveats"
  default_prompt: "Use $tkl-back-running to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
