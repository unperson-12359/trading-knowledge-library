interface:
  display_name: "Slippage tolerance"
  short_description: "Apply Slippage tolerance with evidence and caveats"
  default_prompt: "Use $tkl-slippage-tolerance to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
