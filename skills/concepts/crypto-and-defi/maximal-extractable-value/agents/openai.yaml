interface:
  display_name: "Maximal extractable value"
  short_description: "Apply Maximal extractable value with evidence and caveats"
  default_prompt: "Use $tkl-maximal-extractable-value to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
