interface:
  display_name: "Yield farming"
  short_description: "Apply Yield farming with evidence and caveats"
  default_prompt: "Use $tkl-yield-farming to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
