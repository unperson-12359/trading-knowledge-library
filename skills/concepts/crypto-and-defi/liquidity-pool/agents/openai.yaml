interface:
  display_name: "Liquidity pool"
  short_description: "Apply Liquidity pool with evidence and caveats"
  default_prompt: "Use $tkl-liquidity-pool to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
