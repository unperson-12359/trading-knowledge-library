interface:
  display_name: "Realized capitalization"
  short_description: "Apply Realized capitalization with evidence and caveats"
  default_prompt: "Use $tkl-realized-capitalization to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
