interface:
  display_name: "Blockchain"
  short_description: "Apply Blockchain with evidence and caveats"
  default_prompt: "Use $tkl-blockchain to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
