interface:
  display_name: "Liquidation cascade"
  short_description: "Apply Liquidation cascade with evidence and caveats"
  default_prompt: "Use $tkl-liquidation-cascade to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
