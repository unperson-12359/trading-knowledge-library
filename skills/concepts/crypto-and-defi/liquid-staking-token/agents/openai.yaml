interface:
  display_name: "Liquid staking token"
  short_description: "Apply Liquid staking token with evidence and caveats"
  default_prompt: "Use $tkl-liquid-staking-token to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
