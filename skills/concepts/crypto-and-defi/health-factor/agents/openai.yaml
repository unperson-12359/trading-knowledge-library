interface:
  display_name: "Health factor"
  short_description: "Apply Health factor with evidence and caveats"
  default_prompt: "Use $tkl-health-factor to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
