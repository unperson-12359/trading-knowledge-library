interface:
  display_name: "Protocol fees"
  short_description: "Apply Protocol fees with evidence and caveats"
  default_prompt: "Use $tkl-protocol-fees to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
