interface:
  display_name: "Optimistic rollup"
  short_description: "Apply Optimistic rollup with evidence and caveats"
  default_prompt: "Use $tkl-optimistic-rollup to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
