interface:
  display_name: "Tokenomics"
  short_description: "Apply Tokenomics with evidence and caveats"
  default_prompt: "Use $tkl-tokenomics to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
