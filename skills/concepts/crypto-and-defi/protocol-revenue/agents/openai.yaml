interface:
  display_name: "Protocol revenue"
  short_description: "Apply Protocol revenue with evidence and caveats"
  default_prompt: "Use $tkl-protocol-revenue to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
