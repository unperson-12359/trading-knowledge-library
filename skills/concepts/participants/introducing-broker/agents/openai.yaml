interface:
  display_name: "Introducing broker"
  short_description: "Apply Introducing broker with evidence and caveats"
  default_prompt: "Use $tkl-introducing-broker to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
