interface:
  display_name: "Futures commission merchant"
  short_description: "Apply Futures commission merchant with evidence and caveats"
  default_prompt: "Use $tkl-futures-commission-merchant to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
