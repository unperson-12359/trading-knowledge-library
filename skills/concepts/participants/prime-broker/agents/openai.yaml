interface:
  display_name: "Prime broker"
  short_description: "Apply Prime broker with evidence and caveats"
  default_prompt: "Use $tkl-prime-broker to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
