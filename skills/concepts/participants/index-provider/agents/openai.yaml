interface:
  display_name: "Index provider"
  short_description: "Apply Index provider with evidence and caveats"
  default_prompt: "Use $tkl-index-provider to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
