interface:
  display_name: "Clearing member"
  short_description: "Apply Clearing member with evidence and caveats"
  default_prompt: "Use $tkl-clearing-member to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
