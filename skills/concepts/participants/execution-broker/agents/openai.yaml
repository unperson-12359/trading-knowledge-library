interface:
  display_name: "Execution broker"
  short_description: "Apply Execution broker with evidence and caveats"
  default_prompt: "Use $tkl-execution-broker to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
