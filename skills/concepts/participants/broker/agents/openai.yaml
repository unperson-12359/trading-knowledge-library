interface:
  display_name: "Broker"
  short_description: "Apply Broker with evidence and caveats"
  default_prompt: "Use $tkl-broker to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
