interface:
  display_name: "Central counterparty"
  short_description: "Apply Central counterparty with evidence and caveats"
  default_prompt: "Use $tkl-central-counterparty to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
