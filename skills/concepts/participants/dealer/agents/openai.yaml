interface:
  display_name: "Dealer"
  short_description: "Apply Dealer with evidence and caveats"
  default_prompt: "Use $tkl-dealer to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
