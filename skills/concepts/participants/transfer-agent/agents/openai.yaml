interface:
  display_name: "Transfer agent"
  short_description: "Apply Transfer agent with evidence and caveats"
  default_prompt: "Use $tkl-transfer-agent to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
