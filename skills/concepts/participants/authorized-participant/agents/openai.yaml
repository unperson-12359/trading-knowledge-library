interface:
  display_name: "Authorized participant"
  short_description: "Apply Authorized participant with evidence and caveats"
  default_prompt: "Use $tkl-authorized-participant to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
