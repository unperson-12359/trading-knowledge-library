interface:
  display_name: "Scale order"
  short_description: "Apply Scale order with evidence and caveats"
  default_prompt: "Use $tkl-scale-order to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
