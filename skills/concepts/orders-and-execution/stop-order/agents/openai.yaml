interface:
  display_name: "Stop order"
  short_description: "Apply Stop order with evidence and caveats"
  default_prompt: "Use $tkl-stop-order to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
