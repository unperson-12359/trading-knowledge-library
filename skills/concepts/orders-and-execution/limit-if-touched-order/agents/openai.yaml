interface:
  display_name: "Limit-if-touched order"
  short_description: "Apply Limit-if-touched order with evidence and caveats"
  default_prompt: "Use $tkl-limit-if-touched-order to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
