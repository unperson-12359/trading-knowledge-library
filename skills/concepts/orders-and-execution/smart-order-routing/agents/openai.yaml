interface:
  display_name: "Smart order routing"
  short_description: "Apply Smart order routing with evidence and caveats"
  default_prompt: "Use $tkl-smart-order-routing to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
