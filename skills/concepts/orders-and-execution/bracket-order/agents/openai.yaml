interface:
  display_name: "Bracket order"
  short_description: "Apply Bracket order with evidence and caveats"
  default_prompt: "Use $tkl-bracket-order to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
