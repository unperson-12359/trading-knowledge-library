interface:
  display_name: "Immediate-or-cancel order"
  short_description: "Apply Immediate-or-cancel order with evidence and caveats"
  default_prompt: "Use $tkl-immediate-or-cancel-order to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
