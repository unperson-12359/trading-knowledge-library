interface:
  display_name: "Conditional order"
  short_description: "Apply Conditional order with evidence and caveats"
  default_prompt: "Use $tkl-conditional-order to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
