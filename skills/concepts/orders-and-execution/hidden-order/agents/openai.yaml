interface:
  display_name: "Hidden order"
  short_description: "Apply Hidden order with evidence and caveats"
  default_prompt: "Use $tkl-hidden-order to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
