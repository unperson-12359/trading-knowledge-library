interface:
  display_name: "Order routing"
  short_description: "Apply Order routing with evidence and caveats"
  default_prompt: "Use $tkl-order-routing to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
