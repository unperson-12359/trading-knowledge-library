interface:
  display_name: "Delay cost"
  short_description: "Apply Delay cost with evidence and caveats"
  default_prompt: "Use $tkl-delay-cost to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
