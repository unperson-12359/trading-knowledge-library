interface:
  display_name: "Exchange fee"
  short_description: "Apply Exchange fee with evidence and caveats"
  default_prompt: "Use $tkl-exchange-fee to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
