interface:
  display_name: "Implementation shortfall algorithm"
  short_description: "Apply Implementation shortfall algorithm with evidence"
  default_prompt: "Use $tkl-implementation-shortfall-algorithm to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
