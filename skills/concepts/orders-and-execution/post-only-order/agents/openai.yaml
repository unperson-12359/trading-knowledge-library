interface:
  display_name: "Post-only order"
  short_description: "Apply Post-only order with evidence and caveats"
  default_prompt: "Use $tkl-post-only-order to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
