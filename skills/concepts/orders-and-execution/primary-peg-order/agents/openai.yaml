interface:
  display_name: "Primary peg order"
  short_description: "Apply Primary peg order with evidence and caveats"
  default_prompt: "Use $tkl-primary-peg-order to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
