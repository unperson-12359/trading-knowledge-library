interface:
  display_name: "Order rejection"
  short_description: "Apply Order rejection with evidence and caveats"
  default_prompt: "Use $tkl-order-rejection to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
