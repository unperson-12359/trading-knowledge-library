interface:
  display_name: "TWAP order"
  short_description: "Apply TWAP order with evidence and caveats"
  default_prompt: "Use $tkl-twap-order to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
