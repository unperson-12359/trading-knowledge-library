interface:
  display_name: "Explicit transaction cost"
  short_description: "Apply Explicit transaction cost with evidence and caveats"
  default_prompt: "Use $tkl-explicit-transaction-cost to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
