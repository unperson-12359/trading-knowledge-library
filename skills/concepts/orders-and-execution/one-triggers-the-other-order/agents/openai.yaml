interface:
  display_name: "One-triggers-the-other order"
  short_description: "Apply One-triggers-the-other order with evidence and caveats"
  default_prompt: "Use $tkl-one-triggers-the-other-order to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
