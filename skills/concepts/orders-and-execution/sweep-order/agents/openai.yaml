interface:
  display_name: "Sweep order"
  short_description: "Apply Sweep order with evidence and caveats"
  default_prompt: "Use $tkl-sweep-order to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
