interface:
  display_name: "Reserve order"
  short_description: "Apply Reserve order with evidence and caveats"
  default_prompt: "Use $tkl-reserve-order to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
