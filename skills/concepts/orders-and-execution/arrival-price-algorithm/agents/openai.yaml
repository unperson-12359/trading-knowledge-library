interface:
  display_name: "Arrival price algorithm"
  short_description: "Apply Arrival price algorithm with evidence and caveats"
  default_prompt: "Use $tkl-arrival-price-algorithm to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
