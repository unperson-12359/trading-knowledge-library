interface:
  display_name: "Close-only order"
  short_description: "Apply Close-only order with evidence and caveats"
  default_prompt: "Use $tkl-close-only-order to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
