interface:
  display_name: "Market-if-touched order"
  short_description: "Apply Market-if-touched order with evidence and caveats"
  default_prompt: "Use $tkl-market-if-touched-order to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
