interface:
  display_name: "Stop-limit order"
  short_description: "Apply Stop-limit order with evidence and caveats"
  default_prompt: "Use $tkl-stop-limit-order to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
