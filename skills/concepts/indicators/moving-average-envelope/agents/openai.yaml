interface:
  display_name: "Moving average envelope"
  short_description: "Apply Moving average envelope with evidence and caveats"
  default_prompt: "Use $tkl-moving-average-envelope to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
