interface:
  display_name: "Volume-weighted moving average"
  short_description: "Apply Volume-weighted moving average with evidence and caveats"
  default_prompt: "Use $tkl-volume-weighted-moving-average to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
