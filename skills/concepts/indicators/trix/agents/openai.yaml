interface:
  display_name: "TRIX"
  short_description: "Apply TRIX with evidence and caveats"
  default_prompt: "Use $tkl-trix to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
