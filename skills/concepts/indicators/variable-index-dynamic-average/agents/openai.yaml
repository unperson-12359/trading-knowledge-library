interface:
  display_name: "Variable index dynamic average"
  short_description: "Apply Variable index dynamic average with evidence and caveats"
  default_prompt: "Use $tkl-variable-index-dynamic-average to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
