interface:
  display_name: "Negative volume index"
  short_description: "Apply Negative volume index with evidence and caveats"
  default_prompt: "Use $tkl-negative-volume-index to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
