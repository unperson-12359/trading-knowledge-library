interface:
  display_name: "Detrended price oscillator"
  short_description: "Apply Detrended price oscillator with evidence and caveats"
  default_prompt: "Use $tkl-detrended-price-oscillator to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
