interface:
  display_name: "Force index"
  short_description: "Apply Force index with evidence and caveats"
  default_prompt: "Use $tkl-force-index to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
