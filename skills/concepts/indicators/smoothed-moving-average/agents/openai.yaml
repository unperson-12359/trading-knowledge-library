interface:
  display_name: "Smoothed moving average"
  short_description: "Apply Smoothed moving average with evidence and caveats"
  default_prompt: "Use $tkl-smoothed-moving-average to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
