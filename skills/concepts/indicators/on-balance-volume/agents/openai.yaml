interface:
  display_name: "On-balance volume"
  short_description: "Apply On-balance volume with evidence and caveats"
  default_prompt: "Use $tkl-on-balance-volume to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
