interface:
  display_name: "Commodity channel index"
  short_description: "Apply Commodity channel index with evidence and caveats"
  default_prompt: "Use $tkl-commodity-channel-index to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
