interface:
  display_name: "Average true range"
  short_description: "Apply Average true range with evidence and caveats"
  default_prompt: "Use $tkl-average-true-range to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
