interface:
  display_name: "Positive volume index"
  short_description: "Apply Positive volume index with evidence and caveats"
  default_prompt: "Use $tkl-positive-volume-index to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
