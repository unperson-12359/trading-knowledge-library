interface:
  display_name: "Mass index"
  short_description: "Apply Mass index with evidence and caveats"
  default_prompt: "Use $tkl-mass-index to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
