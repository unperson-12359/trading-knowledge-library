interface:
  display_name: "Volume oscillator"
  short_description: "Apply Volume oscillator with evidence and caveats"
  default_prompt: "Use $tkl-volume-oscillator to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
