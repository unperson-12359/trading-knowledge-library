interface:
  display_name: "Relative vigor index"
  short_description: "Apply Relative vigor index with evidence and caveats"
  default_prompt: "Use $tkl-relative-vigor-index to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
