interface:
  display_name: "Elder Ray index"
  short_description: "Apply Elder Ray index with evidence and caveats"
  default_prompt: "Use $tkl-elder-ray-index to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
