interface:
  display_name: "Volume-weighted average price"
  short_description: "Apply Volume-weighted average price with evidence and caveats"
  default_prompt: "Use $tkl-volume-weighted-average-price to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
