interface:
  display_name: "Momentum indicator"
  short_description: "Apply Momentum indicator with evidence and caveats"
  default_prompt: "Use $tkl-momentum-indicator to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
