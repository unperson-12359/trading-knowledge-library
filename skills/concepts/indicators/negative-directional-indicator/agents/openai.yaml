interface:
  display_name: "Negative directional indicator"
  short_description: "Apply Negative directional indicator with evidence and caveats"
  default_prompt: "Use $tkl-negative-directional-indicator to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
