interface:
  display_name: "Simple moving average"
  short_description: "Apply Simple moving average with evidence and caveats"
  default_prompt: "Use $tkl-simple-moving-average to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
