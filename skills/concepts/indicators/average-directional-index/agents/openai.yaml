interface:
  display_name: "Average directional index"
  short_description: "Apply Average directional index with evidence and caveats"
  default_prompt: "Use $tkl-average-directional-index to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
