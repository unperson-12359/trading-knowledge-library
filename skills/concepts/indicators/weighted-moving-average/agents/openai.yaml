interface:
  display_name: "Weighted moving average"
  short_description: "Apply Weighted moving average with evidence and caveats"
  default_prompt: "Use $tkl-weighted-moving-average to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
