interface:
  display_name: "Relative volume"
  short_description: "Apply Relative volume with evidence and caveats"
  default_prompt: "Use $tkl-relative-volume to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
