interface:
  display_name: "Accumulation distribution line"
  short_description: "Apply Accumulation distribution line with evidence and caveats"
  default_prompt: "Use $tkl-accumulation-distribution-line to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
