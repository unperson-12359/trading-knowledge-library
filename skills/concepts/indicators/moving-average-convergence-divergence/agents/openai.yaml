interface:
  display_name: "Moving average convergence divergence"
  short_description: "Apply Moving average convergence divergence with evidence"
  default_prompt: "Use $tkl-moving-average-convergence-divergence to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
