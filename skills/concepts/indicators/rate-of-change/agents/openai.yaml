interface:
  display_name: "Rate of change"
  short_description: "Apply Rate of change with evidence and caveats"
  default_prompt: "Use $tkl-rate-of-change to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
