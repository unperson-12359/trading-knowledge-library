interface:
  display_name: "Autoencoder"
  short_description: "Apply Autoencoder with evidence and caveats"
  default_prompt: "Use $tkl-autoencoder to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
