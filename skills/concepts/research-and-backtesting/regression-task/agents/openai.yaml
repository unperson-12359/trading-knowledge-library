interface:
  display_name: "Regression task"
  short_description: "Apply Regression task with evidence and caveats"
  default_prompt: "Use $tkl-regression-task to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
