interface:
  display_name: "Purged cross-validation"
  short_description: "Apply Purged cross-validation with evidence and caveats"
  default_prompt: "Use $tkl-purged-cross-validation to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
