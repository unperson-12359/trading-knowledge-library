interface:
  display_name: "XGBoost"
  short_description: "Apply XGBoost with evidence and caveats"
  default_prompt: "Use $tkl-xgboost to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
