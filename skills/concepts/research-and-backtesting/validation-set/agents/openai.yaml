interface:
  display_name: "Validation set"
  short_description: "Apply Validation set with evidence and caveats"
  default_prompt: "Use $tkl-validation-set to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
