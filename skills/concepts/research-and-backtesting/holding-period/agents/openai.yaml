interface:
  display_name: "Holding period"
  short_description: "Apply Holding period with evidence and caveats"
  default_prompt: "Use $tkl-holding-period to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
