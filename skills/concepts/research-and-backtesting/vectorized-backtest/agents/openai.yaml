interface:
  display_name: "Vectorized backtest"
  short_description: "Apply Vectorized backtest with evidence and caveats"
  default_prompt: "Use $tkl-vectorized-backtest to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
