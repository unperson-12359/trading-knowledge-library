interface:
  display_name: "Sensitivity test"
  short_description: "Apply Sensitivity test with evidence and caveats"
  default_prompt: "Use $tkl-sensitivity-test to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
