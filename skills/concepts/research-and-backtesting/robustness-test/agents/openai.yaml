interface:
  display_name: "Robustness test"
  short_description: "Apply Robustness test with evidence and caveats"
  default_prompt: "Use $tkl-robustness-test to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
