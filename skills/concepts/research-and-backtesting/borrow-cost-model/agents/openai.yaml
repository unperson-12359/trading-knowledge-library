interface:
  display_name: "Borrow-cost model"
  short_description: "Apply Borrow-cost model with evidence and caveats"
  default_prompt: "Use $tkl-borrow-cost-model to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
