interface:
  display_name: "Early stopping"
  short_description: "Apply Early stopping with evidence and caveats"
  default_prompt: "Use $tkl-early-stopping to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
