interface:
  display_name: "Target variable"
  short_description: "Apply Target variable with evidence and caveats"
  default_prompt: "Use $tkl-target-variable to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
