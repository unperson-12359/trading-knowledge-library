interface:
  display_name: "K-means clustering"
  short_description: "Apply K-means clustering with evidence and caveats"
  default_prompt: "Use $tkl-k-means-clustering to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
