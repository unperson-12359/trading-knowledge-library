interface:
  display_name: "Test set"
  short_description: "Apply Test set with evidence and caveats"
  default_prompt: "Use $tkl-test-set to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
