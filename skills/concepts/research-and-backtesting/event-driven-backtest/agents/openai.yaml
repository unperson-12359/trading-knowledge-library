interface:
  display_name: "Event-driven backtest"
  short_description: "Apply Event-driven backtest with evidence and caveats"
  default_prompt: "Use $tkl-event-driven-backtest to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
