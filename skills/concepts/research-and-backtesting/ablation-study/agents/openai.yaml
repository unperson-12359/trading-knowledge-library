interface:
  display_name: "Ablation study"
  short_description: "Apply Ablation study with evidence and caveats"
  default_prompt: "Use $tkl-ablation-study to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
