interface:
  display_name: "Root mean squared error"
  short_description: "Apply Root mean squared error with evidence and caveats"
  default_prompt: "Use $tkl-root-mean-squared-error to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
