interface:
  display_name: "Execution simulator"
  short_description: "Apply Execution simulator with evidence and caveats"
  default_prompt: "Use $tkl-execution-simulator to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
