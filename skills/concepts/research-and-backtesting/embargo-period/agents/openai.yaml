interface:
  display_name: "Embargo period"
  short_description: "Apply Embargo period with evidence and caveats"
  default_prompt: "Use $tkl-embargo-period to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
