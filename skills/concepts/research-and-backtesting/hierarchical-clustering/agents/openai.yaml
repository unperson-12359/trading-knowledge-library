interface:
  display_name: "Hierarchical clustering"
  short_description: "Apply Hierarchical clustering with evidence and caveats"
  default_prompt: "Use $tkl-hierarchical-clustering to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
