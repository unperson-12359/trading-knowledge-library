interface:
  display_name: "Rolling-window analysis"
  short_description: "Apply Rolling-window analysis with evidence and caveats"
  default_prompt: "Use $tkl-rolling-window-analysis to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
