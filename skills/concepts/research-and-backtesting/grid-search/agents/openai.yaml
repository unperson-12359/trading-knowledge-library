interface:
  display_name: "Grid search"
  short_description: "Apply Grid search with evidence and caveats"
  default_prompt: "Use $tkl-grid-search to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
