interface:
  display_name: "Recall"
  short_description: "Apply Recall with evidence and caveats"
  default_prompt: "Use $tkl-recall to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
