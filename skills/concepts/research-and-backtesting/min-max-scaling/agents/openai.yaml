interface:
  display_name: "Min-max scaling"
  short_description: "Apply Min-max scaling with evidence and caveats"
  default_prompt: "Use $tkl-min-max-scaling to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
