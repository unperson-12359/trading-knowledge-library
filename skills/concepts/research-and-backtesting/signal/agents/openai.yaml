interface:
  display_name: "Signal"
  short_description: "Apply Signal with evidence and caveats"
  default_prompt: "Use $tkl-signal to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
