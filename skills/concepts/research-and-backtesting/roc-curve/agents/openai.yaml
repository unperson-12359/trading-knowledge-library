interface:
  display_name: "ROC curve"
  short_description: "Apply ROC curve with evidence and caveats"
  default_prompt: "Use $tkl-roc-curve to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
