interface:
  display_name: "Funding-cost model"
  short_description: "Apply Funding-cost model with evidence and caveats"
  default_prompt: "Use $tkl-funding-cost-model to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
