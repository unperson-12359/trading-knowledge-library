interface:
  display_name: "Objective function"
  short_description: "Apply Objective function with evidence and caveats"
  default_prompt: "Use $tkl-objective-function to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
