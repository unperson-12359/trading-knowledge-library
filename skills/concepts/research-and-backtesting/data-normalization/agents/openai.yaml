interface:
  display_name: "Data normalization"
  short_description: "Apply Data normalization with evidence and caveats"
  default_prompt: "Use $tkl-data-normalization to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
