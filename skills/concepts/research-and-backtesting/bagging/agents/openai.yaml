interface:
  display_name: "Bagging"
  short_description: "Apply Bagging with evidence and caveats"
  default_prompt: "Use $tkl-bagging to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
