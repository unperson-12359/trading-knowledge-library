interface:
  display_name: "Brier score"
  short_description: "Apply Brier score with evidence and caveats"
  default_prompt: "Use $tkl-brier-score to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
