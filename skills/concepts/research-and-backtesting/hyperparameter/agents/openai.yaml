interface:
  display_name: "Hyperparameter"
  short_description: "Apply Hyperparameter with evidence and caveats"
  default_prompt: "Use $tkl-hyperparameter to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
