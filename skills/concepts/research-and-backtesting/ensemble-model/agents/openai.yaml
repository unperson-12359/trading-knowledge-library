interface:
  display_name: "Ensemble model"
  short_description: "Apply Ensemble model with evidence and caveats"
  default_prompt: "Use $tkl-ensemble-model to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
