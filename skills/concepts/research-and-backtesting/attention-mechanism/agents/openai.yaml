interface:
  display_name: "Attention mechanism"
  short_description: "Apply Attention mechanism with evidence and caveats"
  default_prompt: "Use $tkl-attention-mechanism to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
