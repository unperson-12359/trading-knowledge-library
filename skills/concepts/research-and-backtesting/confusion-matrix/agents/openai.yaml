interface:
  display_name: "Confusion matrix"
  short_description: "Apply Confusion matrix with evidence and caveats"
  default_prompt: "Use $tkl-confusion-matrix to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
