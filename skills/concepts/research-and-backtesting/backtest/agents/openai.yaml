interface:
  display_name: "Backtest"
  short_description: "Apply Backtest with evidence and caveats"
  default_prompt: "Use $tkl-backtest to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
