interface:
  display_name: "Strategy decay"
  short_description: "Apply Strategy decay with evidence and caveats"
  default_prompt: "Use $tkl-strategy-decay to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
