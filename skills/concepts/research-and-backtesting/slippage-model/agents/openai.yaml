interface:
  display_name: "Slippage model"
  short_description: "Apply Slippage model with evidence and caveats"
  default_prompt: "Use $tkl-slippage-model to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
