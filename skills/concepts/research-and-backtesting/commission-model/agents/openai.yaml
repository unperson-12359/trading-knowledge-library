interface:
  display_name: "Commission model"
  short_description: "Apply Commission model with evidence and caveats"
  default_prompt: "Use $tkl-commission-model to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
