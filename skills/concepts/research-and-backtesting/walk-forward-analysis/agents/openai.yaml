interface:
  display_name: "Walk-forward analysis"
  short_description: "Apply Walk-forward analysis with evidence and caveats"
  default_prompt: "Use $tkl-walk-forward-analysis to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
