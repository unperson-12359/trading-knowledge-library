interface:
  display_name: "Expanding-window analysis"
  short_description: "Apply Expanding-window analysis with evidence and caveats"
  default_prompt: "Use $tkl-expanding-window-analysis to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
