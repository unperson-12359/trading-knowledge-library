interface:
  display_name: "Precision"
  short_description: "Apply Precision with evidence and caveats"
  default_prompt: "Use $tkl-precision to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
