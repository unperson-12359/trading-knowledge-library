interface:
  display_name: "Constraint"
  short_description: "Apply Constraint with evidence and caveats"
  default_prompt: "Use $tkl-constraint to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
