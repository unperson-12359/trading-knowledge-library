interface:
  display_name: "Random search"
  short_description: "Apply Random search with evidence and caveats"
  default_prompt: "Use $tkl-random-search to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
