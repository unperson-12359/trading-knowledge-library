interface:
  display_name: "F1 score"
  short_description: "Apply F1 score with evidence and caveats"
  default_prompt: "Use $tkl-f1-score to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
