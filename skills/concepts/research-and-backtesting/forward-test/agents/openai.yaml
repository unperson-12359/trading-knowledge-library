interface:
  display_name: "Forward test"
  short_description: "Apply Forward test with evidence and caveats"
  default_prompt: "Use $tkl-forward-test to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
