interface:
  display_name: "Recurrent neural network"
  short_description: "Apply Recurrent neural network with evidence and caveats"
  default_prompt: "Use $tkl-recurrent-neural-network to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
