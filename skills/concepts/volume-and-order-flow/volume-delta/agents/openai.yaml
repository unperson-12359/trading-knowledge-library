interface:
  display_name: "Volume delta"
  short_description: "Apply Volume delta with evidence and caveats"
  default_prompt: "Use $tkl-volume-delta to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
