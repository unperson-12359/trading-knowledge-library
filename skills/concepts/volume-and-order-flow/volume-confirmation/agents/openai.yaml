interface:
  display_name: "Volume confirmation"
  short_description: "Apply Volume confirmation with evidence and caveats"
  default_prompt: "Use $tkl-volume-confirmation to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
