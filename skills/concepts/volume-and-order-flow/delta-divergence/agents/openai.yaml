interface:
  display_name: "Delta divergence"
  short_description: "Apply Delta divergence with evidence and caveats"
  default_prompt: "Use $tkl-delta-divergence to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
