interface:
  display_name: "Composite profile"
  short_description: "Apply Composite profile with evidence and caveats"
  default_prompt: "Use $tkl-composite-profile to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
