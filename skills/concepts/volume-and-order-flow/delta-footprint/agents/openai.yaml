interface:
  display_name: "Delta footprint"
  short_description: "Apply Delta footprint with evidence and caveats"
  default_prompt: "Use $tkl-delta-footprint to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
