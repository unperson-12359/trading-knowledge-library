interface:
  display_name: "Volume value area"
  short_description: "Apply Volume value area with evidence and caveats"
  default_prompt: "Use $tkl-volume-value-area to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
