interface:
  display_name: "Session profile"
  short_description: "Apply Session profile with evidence and caveats"
  default_prompt: "Use $tkl-session-profile to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
