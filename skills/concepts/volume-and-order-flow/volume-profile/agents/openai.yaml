interface:
  display_name: "Volume profile"
  short_description: "Apply Volume profile with evidence and caveats"
  default_prompt: "Use $tkl-volume-profile to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
