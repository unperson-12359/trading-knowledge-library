interface:
  display_name: "Volume footprint"
  short_description: "Apply Volume footprint with evidence and caveats"
  default_prompt: "Use $tkl-volume-footprint to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
