interface:
  display_name: "Visible-range volume profile"
  short_description: "Apply Visible-range volume profile with evidence and caveats"
  default_prompt: "Use $tkl-visible-range-volume-profile to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
