interface:
  display_name: "Cumulative volume delta"
  short_description: "Apply Cumulative volume delta with evidence and caveats"
  default_prompt: "Use $tkl-cumulative-volume-delta to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
