interface:
  display_name: "Rejection"
  short_description: "Apply Rejection with evidence and caveats"
  default_prompt: "Use $tkl-rejection to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
