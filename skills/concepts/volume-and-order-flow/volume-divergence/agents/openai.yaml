interface:
  display_name: "Volume divergence"
  short_description: "Apply Volume divergence with evidence and caveats"
  default_prompt: "Use $tkl-volume-divergence to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
