interface:
  display_name: "Fixed-range volume profile"
  short_description: "Apply Fixed-range volume profile with evidence and caveats"
  default_prompt: "Use $tkl-fixed-range-volume-profile to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
