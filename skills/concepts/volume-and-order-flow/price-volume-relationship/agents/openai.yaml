interface:
  display_name: "Price-volume relationship"
  short_description: "Apply Price-volume relationship with evidence and caveats"
  default_prompt: "Use $tkl-price-volume-relationship to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
