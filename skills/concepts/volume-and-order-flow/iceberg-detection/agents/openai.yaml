interface:
  display_name: "Iceberg detection"
  short_description: "Apply Iceberg detection with evidence and caveats"
  default_prompt: "Use $tkl-iceberg-detection to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
