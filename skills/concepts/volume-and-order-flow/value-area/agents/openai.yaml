interface:
  display_name: "Value area"
  short_description: "Apply Value area with evidence and caveats"
  default_prompt: "Use $tkl-value-area to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
