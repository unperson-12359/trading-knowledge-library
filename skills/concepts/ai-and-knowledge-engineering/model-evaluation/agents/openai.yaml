interface:
  display_name: "Model evaluation"
  short_description: "Apply Model evaluation with evidence and caveats"
  default_prompt: "Use $tkl-model-evaluation to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
