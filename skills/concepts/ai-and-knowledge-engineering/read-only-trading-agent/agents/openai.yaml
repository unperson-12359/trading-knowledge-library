interface:
  display_name: "Read-only trading agent"
  short_description: "Apply Read-only trading agent with evidence and caveats"
  default_prompt: "Use $tkl-read-only-trading-agent to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
