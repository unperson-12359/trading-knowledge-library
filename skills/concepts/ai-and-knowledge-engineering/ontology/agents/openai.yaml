interface:
  display_name: "Ontology"
  short_description: "Apply Ontology with evidence and caveats"
  default_prompt: "Use $tkl-ontology to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
