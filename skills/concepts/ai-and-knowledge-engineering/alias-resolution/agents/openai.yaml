interface:
  display_name: "Alias resolution"
  short_description: "Apply Alias resolution with evidence and caveats"
  default_prompt: "Use $tkl-alias-resolution to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
