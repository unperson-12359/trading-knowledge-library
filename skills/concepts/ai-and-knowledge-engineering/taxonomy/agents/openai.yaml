interface:
  display_name: "Taxonomy"
  short_description: "Apply Taxonomy with evidence and caveats"
  default_prompt: "Use $tkl-taxonomy to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
