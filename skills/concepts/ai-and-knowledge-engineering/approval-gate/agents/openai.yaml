interface:
  display_name: "Approval gate"
  short_description: "Apply Approval gate with evidence and caveats"
  default_prompt: "Use $tkl-approval-gate to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
