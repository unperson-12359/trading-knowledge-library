interface:
  display_name: "Trust status"
  short_description: "Apply Trust status with evidence and caveats"
  default_prompt: "Use $tkl-trust-status to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
