interface:
  display_name: "Audit log"
  short_description: "Apply Audit log with evidence and caveats"
  default_prompt: "Use $tkl-audit-log to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
