interface:
  display_name: "Embedding"
  short_description: "Apply Embedding with evidence and caveats"
  default_prompt: "Use $tkl-embedding to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
