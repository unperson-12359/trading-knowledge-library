interface:
  display_name: "Source provenance"
  short_description: "Apply Source provenance with evidence and caveats"
  default_prompt: "Use $tkl-source-provenance to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
