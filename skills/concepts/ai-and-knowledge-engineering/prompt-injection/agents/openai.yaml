interface:
  display_name: "Prompt injection"
  short_description: "Apply Prompt injection with evidence and caveats"
  default_prompt: "Use $tkl-prompt-injection to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
