interface:
  display_name: "Structured output"
  short_description: "Apply Structured output with evidence and caveats"
  default_prompt: "Use $tkl-structured-output to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
