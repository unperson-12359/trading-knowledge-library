interface:
  display_name: "Entity linking"
  short_description: "Apply Entity linking with evidence and caveats"
  default_prompt: "Use $tkl-entity-linking to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
