interface:
  display_name: "Model drift"
  short_description: "Apply Model drift with evidence and caveats"
  default_prompt: "Use $tkl-model-drift to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
