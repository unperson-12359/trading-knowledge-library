interface:
  display_name: "Data poisoning"
  short_description: "Apply Data poisoning with evidence and caveats"
  default_prompt: "Use $tkl-data-poisoning to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
