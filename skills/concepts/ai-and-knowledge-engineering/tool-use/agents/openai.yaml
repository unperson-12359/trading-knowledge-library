interface:
  display_name: "Tool use"
  short_description: "Apply Tool use with evidence and caveats"
  default_prompt: "Use $tkl-tool-use to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
