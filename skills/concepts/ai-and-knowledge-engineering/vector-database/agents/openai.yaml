interface:
  display_name: "Vector database"
  short_description: "Apply Vector database with evidence and caveats"
  default_prompt: "Use $tkl-vector-database to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
