interface:
  display_name: "Agent memory"
  short_description: "Apply Agent memory with evidence and caveats"
  default_prompt: "Use $tkl-agent-memory to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
