interface:
  display_name: "Hallucination"
  short_description: "Apply Hallucination with evidence and caveats"
  default_prompt: "Use $tkl-hallucination to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
