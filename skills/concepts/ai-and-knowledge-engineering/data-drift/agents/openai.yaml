interface:
  display_name: "Data drift"
  short_description: "Apply Data drift with evidence and caveats"
  default_prompt: "Use $tkl-data-drift to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
