interface:
  display_name: "Golden dataset"
  short_description: "Apply Golden dataset with evidence and caveats"
  default_prompt: "Use $tkl-golden-dataset to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
