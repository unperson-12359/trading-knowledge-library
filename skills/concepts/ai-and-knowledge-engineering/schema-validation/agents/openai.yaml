interface:
  display_name: "Schema validation"
  short_description: "Apply Schema validation with evidence and caveats"
  default_prompt: "Use $tkl-schema-validation to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
