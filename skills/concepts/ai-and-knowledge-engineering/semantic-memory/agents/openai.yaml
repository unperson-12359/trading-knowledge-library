interface:
  display_name: "Semantic memory"
  short_description: "Apply Semantic memory with evidence and caveats"
  default_prompt: "Use $tkl-semantic-memory to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
