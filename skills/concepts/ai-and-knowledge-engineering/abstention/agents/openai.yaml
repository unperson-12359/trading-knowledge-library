interface:
  display_name: "Abstention"
  short_description: "Apply Abstention with evidence and caveats"
  default_prompt: "Use $tkl-abstention to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
