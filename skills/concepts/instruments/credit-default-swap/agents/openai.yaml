interface:
  display_name: "Credit default swap"
  short_description: "Apply Credit default swap with evidence and caveats"
  default_prompt: "Use $tkl-credit-default-swap to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
