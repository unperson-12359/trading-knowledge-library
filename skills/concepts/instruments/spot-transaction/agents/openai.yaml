interface:
  display_name: "Spot transaction"
  short_description: "Apply Spot transaction with evidence and caveats"
  default_prompt: "Use $tkl-spot-transaction to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
