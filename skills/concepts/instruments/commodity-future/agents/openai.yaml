interface:
  display_name: "Commodity future"
  short_description: "Apply Commodity future with evidence and caveats"
  default_prompt: "Use $tkl-commodity-future to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
