interface:
  display_name: "Barrier option"
  short_description: "Apply Barrier option with evidence and caveats"
  default_prompt: "Use $tkl-barrier-option to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
