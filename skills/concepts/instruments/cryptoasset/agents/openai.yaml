interface:
  display_name: "Cryptoasset"
  short_description: "Apply Cryptoasset with evidence and caveats"
  default_prompt: "Use $tkl-cryptoasset to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
