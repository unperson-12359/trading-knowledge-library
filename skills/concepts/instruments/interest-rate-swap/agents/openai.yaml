interface:
  display_name: "Interest rate swap"
  short_description: "Apply Interest rate swap with evidence and caveats"
  default_prompt: "Use $tkl-interest-rate-swap to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
