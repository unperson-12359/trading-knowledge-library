interface:
  display_name: "Commodity"
  short_description: "Apply Commodity with evidence and caveats"
  default_prompt: "Use $tkl-commodity to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
