interface:
  display_name: "Forward contract"
  short_description: "Apply Forward contract with evidence and caveats"
  default_prompt: "Use $tkl-forward-contract to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
