interface:
  display_name: "Interest rate future"
  short_description: "Apply Interest rate future with evidence and caveats"
  default_prompt: "Use $tkl-interest-rate-future to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
