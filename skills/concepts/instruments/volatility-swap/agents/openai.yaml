interface:
  display_name: "Volatility swap"
  short_description: "Apply Volatility swap with evidence and caveats"
  default_prompt: "Use $tkl-volatility-swap to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
