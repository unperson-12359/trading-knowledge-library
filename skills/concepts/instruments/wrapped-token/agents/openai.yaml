interface:
  display_name: "Wrapped token"
  short_description: "Apply Wrapped token with evidence and caveats"
  default_prompt: "Use $tkl-wrapped-token to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
