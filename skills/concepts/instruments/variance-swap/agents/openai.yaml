interface:
  display_name: "Variance swap"
  short_description: "Apply Variance swap with evidence and caveats"
  default_prompt: "Use $tkl-variance-swap to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
