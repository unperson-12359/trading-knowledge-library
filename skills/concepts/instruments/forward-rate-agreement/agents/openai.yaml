interface:
  display_name: "Forward rate agreement"
  short_description: "Apply Forward rate agreement with evidence and caveats"
  default_prompt: "Use $tkl-forward-rate-agreement to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
