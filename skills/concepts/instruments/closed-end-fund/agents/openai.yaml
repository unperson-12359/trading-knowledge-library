interface:
  display_name: "Closed-end fund"
  short_description: "Apply Closed-end fund with evidence and caveats"
  default_prompt: "Use $tkl-closed-end-fund to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
