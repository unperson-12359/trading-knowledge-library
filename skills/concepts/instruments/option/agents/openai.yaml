interface:
  display_name: "Option"
  short_description: "Apply Option with evidence and caveats"
  default_prompt: "Use $tkl-option to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
