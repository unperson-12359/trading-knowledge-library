interface:
  display_name: "Total return swap"
  short_description: "Apply Total return swap with evidence and caveats"
  default_prompt: "Use $tkl-total-return-swap to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
