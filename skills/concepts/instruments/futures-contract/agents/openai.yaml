interface:
  display_name: "Futures contract"
  short_description: "Apply Futures contract with evidence and caveats"
  default_prompt: "Use $tkl-futures-contract to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
