interface:
  display_name: "Currency swap"
  short_description: "Apply Currency swap with evidence and caveats"
  default_prompt: "Use $tkl-currency-swap to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
