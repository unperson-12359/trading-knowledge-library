interface:
  display_name: "Lookback option"
  short_description: "Apply Lookback option with evidence and caveats"
  default_prompt: "Use $tkl-lookback-option to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
