interface:
  display_name: "Currency pair"
  short_description: "Apply Currency pair with evidence and caveats"
  default_prompt: "Use $tkl-currency-pair to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
