interface:
  display_name: "After-hours session"
  short_description: "Apply After-hours session with evidence and caveats"
  default_prompt: "Use $tkl-after-hours-session to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
