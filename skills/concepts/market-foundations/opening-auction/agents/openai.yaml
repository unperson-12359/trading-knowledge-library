interface:
  display_name: "Opening auction"
  short_description: "Apply Opening auction with evidence and caveats"
  default_prompt: "Use $tkl-opening-auction to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
