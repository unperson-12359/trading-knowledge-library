interface:
  display_name: "Consolidated tape"
  short_description: "Apply Consolidated tape with evidence and caveats"
  default_prompt: "Use $tkl-consolidated-tape to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
