interface:
  display_name: "Overnight session"
  short_description: "Apply Overnight session with evidence and caveats"
  default_prompt: "Use $tkl-overnight-session to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
