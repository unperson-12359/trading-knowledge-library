interface:
  display_name: "Closing auction"
  short_description: "Apply Closing auction with evidence and caveats"
  default_prompt: "Use $tkl-closing-auction to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
