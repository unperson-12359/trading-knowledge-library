interface:
  display_name: "Settlement cycle"
  short_description: "Apply Settlement cycle with evidence and caveats"
  default_prompt: "Use $tkl-settlement-cycle to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
