interface:
  display_name: "Price discovery"
  short_description: "Apply Price discovery with evidence and caveats"
  default_prompt: "Use $tkl-price-discovery to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
