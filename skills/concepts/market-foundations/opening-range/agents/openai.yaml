interface:
  display_name: "Opening range"
  short_description: "Apply Opening range with evidence and caveats"
  default_prompt: "Use $tkl-opening-range to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
