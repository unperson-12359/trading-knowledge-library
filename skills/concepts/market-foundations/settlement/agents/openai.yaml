interface:
  display_name: "Settlement"
  short_description: "Apply Settlement with evidence and caveats"
  default_prompt: "Use $tkl-settlement to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
