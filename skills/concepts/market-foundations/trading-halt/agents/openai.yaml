interface:
  display_name: "Trading halt"
  short_description: "Apply Trading halt with evidence and caveats"
  default_prompt: "Use $tkl-trading-halt to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
