interface:
  display_name: "Initial balance"
  short_description: "Apply Initial balance with evidence and caveats"
  default_prompt: "Use $tkl-initial-balance to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
