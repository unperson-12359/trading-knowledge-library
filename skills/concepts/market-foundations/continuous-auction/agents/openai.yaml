interface:
  display_name: "Continuous auction"
  short_description: "Apply Continuous auction with evidence and caveats"
  default_prompt: "Use $tkl-continuous-auction to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
