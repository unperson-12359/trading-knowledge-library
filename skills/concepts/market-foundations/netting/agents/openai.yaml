interface:
  display_name: "Netting"
  short_description: "Apply Netting with evidence and caveats"
  default_prompt: "Use $tkl-netting to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
