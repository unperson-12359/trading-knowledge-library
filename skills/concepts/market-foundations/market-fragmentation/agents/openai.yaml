interface:
  display_name: "Market fragmentation"
  short_description: "Apply Market fragmentation with evidence and caveats"
  default_prompt: "Use $tkl-market-fragmentation to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
