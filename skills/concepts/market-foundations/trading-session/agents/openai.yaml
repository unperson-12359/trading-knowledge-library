interface:
  display_name: "Trading session"
  short_description: "Apply Trading session with evidence and caveats"
  default_prompt: "Use $tkl-trading-session to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
