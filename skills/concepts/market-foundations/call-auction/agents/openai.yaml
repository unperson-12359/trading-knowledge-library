interface:
  display_name: "Call auction"
  short_description: "Apply Call auction with evidence and caveats"
  default_prompt: "Use $tkl-call-auction to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
