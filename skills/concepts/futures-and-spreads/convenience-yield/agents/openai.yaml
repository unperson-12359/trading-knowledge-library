interface:
  display_name: "Convenience yield"
  short_description: "Apply Convenience yield with evidence and caveats"
  default_prompt: "Use $tkl-convenience-yield to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
