interface:
  display_name: "Open interest change"
  short_description: "Apply Open interest change with evidence and caveats"
  default_prompt: "Use $tkl-open-interest-change to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
