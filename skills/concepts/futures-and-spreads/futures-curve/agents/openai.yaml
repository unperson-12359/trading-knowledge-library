interface:
  display_name: "Futures curve"
  short_description: "Apply Futures curve with evidence and caveats"
  default_prompt: "Use $tkl-futures-curve to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
