interface:
  display_name: "Roll yield"
  short_description: "Apply Roll yield with evidence and caveats"
  default_prompt: "Use $tkl-roll-yield to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
