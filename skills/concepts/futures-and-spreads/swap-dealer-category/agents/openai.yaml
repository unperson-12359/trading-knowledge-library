interface:
  display_name: "Swap dealer category"
  short_description: "Apply Swap dealer category with evidence and caveats"
  default_prompt: "Use $tkl-swap-dealer-category to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
