interface:
  display_name: "Producer merchant processor user"
  short_description: "Apply Producer merchant processor user with evidence and caveats"
  default_prompt: "Use $tkl-producer-merchant-processor-user to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
