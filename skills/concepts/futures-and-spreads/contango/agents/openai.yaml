interface:
  display_name: "Contango"
  short_description: "Apply Contango with evidence and caveats"
  default_prompt: "Use $tkl-contango to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
