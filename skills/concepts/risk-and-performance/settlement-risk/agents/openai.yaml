interface:
  display_name: "Settlement risk"
  short_description: "Apply Settlement risk with evidence and caveats"
  default_prompt: "Use $tkl-settlement-risk to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
