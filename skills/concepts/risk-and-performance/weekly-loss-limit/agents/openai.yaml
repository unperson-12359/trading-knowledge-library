interface:
  display_name: "Weekly loss limit"
  short_description: "Apply Weekly loss limit with evidence and caveats"
  default_prompt: "Use $tkl-weekly-loss-limit to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
