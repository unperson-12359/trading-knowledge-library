interface:
  display_name: "Deleveraging"
  short_description: "Apply Deleveraging with evidence and caveats"
  default_prompt: "Use $tkl-deleveraging to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
