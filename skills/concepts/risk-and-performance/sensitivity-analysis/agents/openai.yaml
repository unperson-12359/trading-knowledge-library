interface:
  display_name: "Sensitivity analysis"
  short_description: "Apply Sensitivity analysis with evidence and caveats"
  default_prompt: "Use $tkl-sensitivity-analysis to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
