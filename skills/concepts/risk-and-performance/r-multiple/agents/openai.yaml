interface:
  display_name: "R-multiple"
  short_description: "Apply R-multiple with evidence and caveats"
  default_prompt: "Use $tkl-r-multiple to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
