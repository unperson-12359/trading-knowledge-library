interface:
  display_name: "Realized volatility"
  short_description: "Apply Realized volatility with evidence and caveats"
  default_prompt: "Use $tkl-realized-volatility to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
