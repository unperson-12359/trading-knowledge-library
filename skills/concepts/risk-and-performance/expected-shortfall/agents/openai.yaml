interface:
  display_name: "Expected shortfall"
  short_description: "Apply Expected shortfall with evidence and caveats"
  default_prompt: "Use $tkl-expected-shortfall to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
