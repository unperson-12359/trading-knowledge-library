interface:
  display_name: "Initial margin"
  short_description: "Apply Initial margin with evidence and caveats"
  default_prompt: "Use $tkl-initial-margin to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
