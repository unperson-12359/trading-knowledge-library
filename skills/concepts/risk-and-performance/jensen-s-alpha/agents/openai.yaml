interface:
  display_name: "Jensen's alpha"
  short_description: "Apply Jensen's alpha with evidence and caveats"
  default_prompt: "Use $tkl-jensen-s-alpha to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
