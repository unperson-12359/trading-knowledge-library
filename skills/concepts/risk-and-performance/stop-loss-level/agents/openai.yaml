interface:
  display_name: "Stop-loss level"
  short_description: "Apply Stop-loss level with evidence and caveats"
  default_prompt: "Use $tkl-stop-loss-level to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
