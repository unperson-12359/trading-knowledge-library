interface:
  display_name: "Tail risk"
  short_description: "Apply Tail risk with evidence and caveats"
  default_prompt: "Use $tkl-tail-risk to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
