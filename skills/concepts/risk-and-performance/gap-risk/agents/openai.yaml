interface:
  display_name: "Gap risk"
  short_description: "Apply Gap risk with evidence and caveats"
  default_prompt: "Use $tkl-gap-risk to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
