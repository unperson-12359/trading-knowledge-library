interface:
  display_name: "Gross exposure"
  short_description: "Apply Gross exposure with evidence and caveats"
  default_prompt: "Use $tkl-gross-exposure to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
