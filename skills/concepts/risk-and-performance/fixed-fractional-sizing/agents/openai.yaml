interface:
  display_name: "Fixed fractional sizing"
  short_description: "Apply Fixed fractional sizing with evidence and caveats"
  default_prompt: "Use $tkl-fixed-fractional-sizing to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
