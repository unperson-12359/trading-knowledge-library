interface:
  display_name: "Liquidation price"
  short_description: "Apply Liquidation price with evidence and caveats"
  default_prompt: "Use $tkl-liquidation-price to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
