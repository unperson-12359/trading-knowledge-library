interface:
  display_name: "Daily loss limit"
  short_description: "Apply Daily loss limit with evidence and caveats"
  default_prompt: "Use $tkl-daily-loss-limit to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
