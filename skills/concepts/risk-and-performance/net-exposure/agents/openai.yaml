interface:
  display_name: "Net exposure"
  short_description: "Apply Net exposure with evidence and caveats"
  default_prompt: "Use $tkl-net-exposure to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
