interface:
  display_name: "Position sizing"
  short_description: "Apply Position sizing with evidence and caveats"
  default_prompt: "Use $tkl-position-sizing to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
