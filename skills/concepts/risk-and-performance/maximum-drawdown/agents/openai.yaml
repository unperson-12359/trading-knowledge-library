interface:
  display_name: "Maximum drawdown"
  short_description: "Apply Maximum drawdown with evidence and caveats"
  default_prompt: "Use $tkl-maximum-drawdown to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
