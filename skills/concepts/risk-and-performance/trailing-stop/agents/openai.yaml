interface:
  display_name: "Trailing stop"
  short_description: "Apply Trailing stop with evidence and caveats"
  default_prompt: "Use $tkl-trailing-stop to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
