interface:
  display_name: "Sortino ratio"
  short_description: "Apply Sortino ratio with evidence and caveats"
  default_prompt: "Use $tkl-sortino-ratio to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
