interface:
  display_name: "Risk-reward ratio"
  short_description: "Apply Risk-reward ratio with evidence and caveats"
  default_prompt: "Use $tkl-risk-reward-ratio to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
