interface:
  display_name: "Risk limit"
  short_description: "Apply Risk limit with evidence and caveats"
  default_prompt: "Use $tkl-risk-limit to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
