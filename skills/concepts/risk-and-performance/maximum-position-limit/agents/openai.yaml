interface:
  display_name: "Maximum position limit"
  short_description: "Apply Maximum position limit with evidence and caveats"
  default_prompt: "Use $tkl-maximum-position-limit to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
