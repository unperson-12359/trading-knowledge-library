interface:
  display_name: "Leverage"
  short_description: "Apply Leverage with evidence and caveats"
  default_prompt: "Use $tkl-leverage to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
