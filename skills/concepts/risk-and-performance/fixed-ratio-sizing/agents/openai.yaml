interface:
  display_name: "Fixed ratio sizing"
  short_description: "Apply Fixed ratio sizing with evidence and caveats"
  default_prompt: "Use $tkl-fixed-ratio-sizing to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
