interface:
  display_name: "Model risk"
  short_description: "Apply Model risk with evidence and caveats"
  default_prompt: "Use $tkl-model-risk to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
