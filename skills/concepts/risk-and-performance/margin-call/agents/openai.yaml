interface:
  display_name: "Margin call"
  short_description: "Apply Margin call with evidence and caveats"
  default_prompt: "Use $tkl-margin-call to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
