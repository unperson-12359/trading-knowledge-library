interface:
  display_name: "Sharpe ratio"
  short_description: "Apply Sharpe ratio with evidence and caveats"
  default_prompt: "Use $tkl-sharpe-ratio to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
