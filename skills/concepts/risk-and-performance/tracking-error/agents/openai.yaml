interface:
  display_name: "Tracking error"
  short_description: "Apply Tracking error with evidence and caveats"
  default_prompt: "Use $tkl-tracking-error to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
