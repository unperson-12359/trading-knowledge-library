interface:
  display_name: "Win rate"
  short_description: "Apply Win rate with evidence and caveats"
  default_prompt: "Use $tkl-win-rate to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
