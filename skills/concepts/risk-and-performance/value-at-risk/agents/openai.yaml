interface:
  display_name: "Value at risk"
  short_description: "Apply Value at risk with evidence and caveats"
  default_prompt: "Use $tkl-value-at-risk to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
