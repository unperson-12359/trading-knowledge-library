interface:
  display_name: "Forced liquidation"
  short_description: "Apply Forced liquidation with evidence and caveats"
  default_prompt: "Use $tkl-forced-liquidation to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
