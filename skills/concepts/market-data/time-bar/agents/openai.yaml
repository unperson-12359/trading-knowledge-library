interface:
  display_name: "Time bar"
  short_description: "Apply Time bar with evidence and caveats"
  default_prompt: "Use $tkl-time-bar to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
