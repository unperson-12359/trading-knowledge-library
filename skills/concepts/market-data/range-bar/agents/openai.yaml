interface:
  display_name: "Range bar"
  short_description: "Apply Range bar with evidence and caveats"
  default_prompt: "Use $tkl-range-bar to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
