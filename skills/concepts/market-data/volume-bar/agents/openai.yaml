interface:
  display_name: "Volume bar"
  short_description: "Apply Volume bar with evidence and caveats"
  default_prompt: "Use $tkl-volume-bar to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
