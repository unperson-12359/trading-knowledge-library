interface:
  display_name: "Rolling minimum"
  short_description: "Apply Rolling minimum with evidence and caveats"
  default_prompt: "Use $tkl-rolling-minimum to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
