interface:
  display_name: "Open price"
  short_description: "Apply Open price with evidence and caveats"
  default_prompt: "Use $tkl-open-price to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
