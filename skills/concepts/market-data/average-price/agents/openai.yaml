interface:
  display_name: "Average price"
  short_description: "Apply Average price with evidence and caveats"
  default_prompt: "Use $tkl-average-price to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
