interface:
  display_name: "Close price"
  short_description: "Apply Close price with evidence and caveats"
  default_prompt: "Use $tkl-close-price to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
