interface:
  display_name: "Low price"
  short_description: "Apply Low price with evidence and caveats"
  default_prompt: "Use $tkl-low-price to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
