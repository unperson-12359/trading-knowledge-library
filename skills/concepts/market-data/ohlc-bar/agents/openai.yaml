interface:
  display_name: "OHLC bar"
  short_description: "Apply OHLC bar with evidence and caveats"
  default_prompt: "Use $tkl-ohlc-bar to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
