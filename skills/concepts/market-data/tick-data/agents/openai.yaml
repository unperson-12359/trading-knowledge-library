interface:
  display_name: "Tick data"
  short_description: "Apply Tick data with evidence and caveats"
  default_prompt: "Use $tkl-tick-data to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
