interface:
  display_name: "Rolling median"
  short_description: "Apply Rolling median with evidence and caveats"
  default_prompt: "Use $tkl-rolling-median to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
