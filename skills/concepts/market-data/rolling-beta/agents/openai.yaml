interface:
  display_name: "Rolling beta"
  short_description: "Apply Rolling beta with evidence and caveats"
  default_prompt: "Use $tkl-rolling-beta to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
