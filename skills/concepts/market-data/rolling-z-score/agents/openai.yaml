interface:
  display_name: "Rolling z-score"
  short_description: "Apply Rolling z-score with evidence and caveats"
  default_prompt: "Use $tkl-rolling-z-score to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
