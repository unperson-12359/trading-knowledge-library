interface:
  display_name: "Rolling correlation"
  short_description: "Apply Rolling correlation with evidence and caveats"
  default_prompt: "Use $tkl-rolling-correlation to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
