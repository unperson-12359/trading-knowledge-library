interface:
  display_name: "Tick bar"
  short_description: "Apply Tick bar with evidence and caveats"
  default_prompt: "Use $tkl-tick-bar to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
