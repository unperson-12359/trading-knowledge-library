interface:
  display_name: "Adjusted close"
  short_description: "Apply Adjusted close with evidence and caveats"
  default_prompt: "Use $tkl-adjusted-close to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
