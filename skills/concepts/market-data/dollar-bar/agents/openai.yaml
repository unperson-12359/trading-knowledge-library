interface:
  display_name: "Dollar bar"
  short_description: "Apply Dollar bar with evidence and caveats"
  default_prompt: "Use $tkl-dollar-bar to answer this question with the concept evidence, failure modes, and citations."
policy:
  allow_implicit_invocation: false
